# Path globs that are NOT expected to resolve inside this repository.
# One glob per line; text after # is the required justification.
#
# There is one failure this linter cannot distinguish on its own: a path in
# ANOTHER repository or on another machine looks exactly like a path in this
# one. docs/arcade-frame-data/CAPTURE.md:214 cites `docs/builde-guide.md`, which
# is a file in the upstream crowded-street/3s-decomp project -- the sentence
# says so explicitly -- and no amount of checking THIS tree can confirm or deny
# it. Listing such a pattern here is a statement that the reference is
# deliberately outward-pointing, not that it is known-good.
#
# Keep entries narrow. A bare `*.py` here would silence the check entirely.

artifacts/**            # uncommitted scratchpad outputs from research sessions
fightcade-fbneo/**      # external FBNeo checkout on the capture rig
crowded-street/**       # the upstream 3s-decomp project, a different repository
3s-decomp/**            # ditto, cited under its short name

# Task #106. The macOS SDK header whose _FORTIFY_SOURCE macro rewrites
# snprintf() to __builtin___snprintf_chk() and so hides -Wformat-truncation
# from every host build. tools/gates/fortify_blind_sweep.py has to name it to
# explain why that gate exists; it lives in Xcode, not in this repository.
*MacOSX.sdk/usr/include/**   # macOS SDK headers, cited to explain host blindness
secure/_stdio.h              # ditto, cited under its short SDK-relative name
