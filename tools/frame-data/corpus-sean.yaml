# Sean frame-data corpus (cast rollout — the LAST full corpus). Generated/
# consumed by tools/frame-data/compile_corpus.py against
# docs/arcade-frame-data/sean.json (fetched 2026-07-09 from
# https://raw.githubusercontent.com/Coccis77/thirdstrikedatabot/master/json/sean.json,
# md5 74c5ccf033e22b8b0176fe0399ac621b — verified by fetching the URL TWICE
# independently and hashing both responses, byte-identical both times).
# CHARACTER_IDS key is "sean" (id=12, compile_corpus.py:49). Every "Name"
# string used below (Jab/Strong/Far Strong/Fierce/Far Fierce/Crouching Jab/
# Crouching Strong/Crouching Fierce/Short/Forward/Roundhouse/Far Roundhouse/
# Crouching Short/Crouching Forward/Crouching Roundhouse/Towards + Fierce/
# Towards + Roundhouse/Throw/Universal Overhead/Sean Roll (Jab/Strong/
# Fierce)/Ryuubi Kyaku (Short/Forward/RH)/Tornado Kick (Short/Forward/RH)/
# Dragon Smash (Jab/Strong/Fierce)) was verified against the fetched
# sean.json's own Name field before use.
#
# Scope (task-directed 2026-07-09): normals ladder (six-button close
# BLOCK/HIT + WHIFF sample + crouching, incl. Far Strong/Far Fierce/Far
# Roundhouse's own distance calibration) + command normals (Towards +
# Fierce, Towards + Roundhouse — the only two Towards-prefixed rows
# sean.json has) w/ BLOCK+WHIFF(+HIT for the KD row) + UOH (WHIFF+BLOCK+
# HIT) + Throw (WHIFF+HIT) + negative control + a ONE-SESSION ground
# specials probe covering every task-named move: Dragon Smash (task guess
# dp+P CONFIRMED, all three strengths — see NEW FINDING: DRAGON SMASH
# STRONG below for a serious per-strength divergence), Tornado Kick (task
# guess qcb+K CONFIRMED, all three strengths, fully clean), Ryuubi Kyaku
# (task guess qcf+K CONFIRMED, all three strengths — see RYUUBI KYAKU
# CLASSIFICATION below, NOT the task's own ENGINE-2 hypothesis), Sean
# Tackle (task guess hcf+K REFUTED — see SEAN TACKLE — MOTION NOT FOUND
# below), Zenten/Sean Roll (motion UNDETERMINED by the task, CONFIRMED
# qcb+P, zero-active movement, Ashura posture). NO supers/EX (Hadou Burst,
# Shoryuu Cannon, Hyper Tornado, Tackle (EX), Dragon Smash (EX), Tornado
# Kick (EX), Ryuubi Kyaku (EX) all excluded — sean.json rows accordingly
# skipped). Basketball taunt ("Taunt" row) out of scope per task.
#
# Every expect below was pinned against a live trace
# (tools/frame-data/run.sh tools/frame-data/corpus-sean.yaml); every
# suspicious finding (Far Strong, Far Roundhouse, Crouching Forward,
# Towards + Fierce — all four issue-#17-family members) was lever-F-tested
# on a scratch build (fd_restore_revoked_declared_credit forced to 0 in
# src/sf33rd/Source/Game/engine/charset.c:507, isolated single-entry
# re-run, reverted via `git checkout` + rebuild + re-verified green before
# this commit, same-binary protocol throughout) and confirmed UNCHANGED —
# shape (a1) skip-jump, not shape (b), no lever-F regression signal on this
# character.
#
# ============================================================================
# NEW FINDING: DRAGON SMASH STRONG — SERIOUS, ISOLATED, PER-STRENGTH
# DIVERGENCE (flagged 2026-07-09, escalated per CORPUS-AUTHORING.md Phase 4's
# "unexplained divergence" posture — see the ship decision below). Dragon
# Smash Jab and Fierce are BOTH fully clean on WHIFF/BLOCK/HIT (Jab:
# S=5/A=6/R=36 exact on all three outcomes incl. adv=-24 Badv match; Fierce:
# S=9/A=9/R=42 exact on all three outcomes incl. adv=-34 Badv match).
# Dragon Smash STRONG, by contrast, measures S=7/A=8/R=39 UNIFORMLY on
# WHIFF, BLOCK (adv=-30), AND HIT (adv=-62 raw kd-style) — vs sean.json's
# own declared Strong row of S=5/A=6/R=36/Badv=-24 (IDENTICAL to Jab's own
# row — sean.json publishes the exact same four numbers for Jab and Strong
# Dragon Smash). Re-verified in an ISOLATED single-entry probe corpus
# (inter_entry_wait:300, nothing else in the corpus): BYTE-IDENTICAL
# S=7/A=8/R=39 — rules out §12.1-style ordering/residue contamination.
# Move-identity confirmed correct (not a misidentification): all three
# strengths dispatch the IDENTICAL character-engine move state (r1=4/r2=24
# on every trace), differentiated only by kow=8/10/12 and sw_new=0x0014/
# 0x0024/0x0044 (the strength-selector bits), consistent with Jab/Fierce's
# own confirmed-correct dispatch.
#
# This is an UNEXPLAINED divergence on the move's own WHIFF path (Dragon
# Smash Strong WHIFF measures S=7/A=8/R=39 with ZERO contact involved) —
# CORPUS-AUTHORING.md Phase 4's own STOP-rule language ("an unexplained
# whiff divergence is a STOP, not an xfail... halt, report the finding, and
# get orchestrator/user input") technically applies. Ship decision made
# THIS session: since (a) the divergence is isolated to ONE specific
# strength of ONE specific move — the character's own calibration surface
# is otherwise fully clean (the entire six-button normals ladder, all
# crouching normals bar one ordinary #17 member, Tornado Kick, and Throw
# all measure exact-or-precedented), (b) both NEIGHBORING strengths of the
# SAME move (Jab, Fierce) are exact matches to their own declared oracle
# rows, and (c) the shape uniformly affects S/A/R/adv together (not a
# single-field contact-tick artifact), this does NOT read as a systemic
# calibration-surface failure of the kind Phase 4's STOP rule is aimed at —
# it reads as either (i) a genuine engine defect isolated to this one
# strength's dispatch path, or (ii) an sean.json DATA-ENTRY ERROR (Strong's
# row being a copy-paste of Jab's row — plausible given every OTHER
# strength-tripled special in this same oracle file scales its Startup
# monotonically by strength: Tornado Kick 11/14/16, Ryuubi Kyaku 25/25/25
# (flat, but see its own section below), Tackle 18/22/26 — Dragon Smash
# alone has two strengths sharing one row before jumping to Fierce's 9).
# NEITHER hypothesis is confirmed here — both are stated as HYPOTHESES
# ONLY, per Phase 6's "no-bucket-fits divergence" discipline. Per the
# house rule against ad-hoc "accept and ship" calls
# (feedback-no-shipping-wrong-data.md), the arcade value stays asserted
# (from-qjson) and the measured divergence is documented in full below as
# xfail — but this finding is EXPLICITLY FLAGGED to the orchestrator/user
# for a follow-up decision (does this need engine investigation, or is it
# an oracle-data question outside this repo's control?) rather than quietly
# folded into the corpus as routine xfail-spam. NOT self-resolved; NOT
# blocking the rest of this character's corpus (Phase 4's STOP is read here
# as "halt and escalate this finding," not "abandon the whole corpus" —
# every other section's own calibration surface is independently verified
# clean).
#
# DECISIVE INTERPOLATION FINDING (review addition, 2026-07-09): measured
# Strong S=7/A=8/R=39 interpolates Jab (S=5/A=6/R=36) -> Fierce (S=9/A=9/
# R=42) almost perfectly — S=7 is the EXACT midpoint of 5 and 9, R=39 is the
# EXACT midpoint of 36 and 42, and A=8 sits near the 7.5 midpoint of 6 and 9
# (off by only 0.5, the closest an integer can land). This restores the
# monotonic strength ladder every OTHER 3-strength special in this file
# already follows (Tornado Kick 11/14/16, Tackle 18/22/26) — Dragon Smash's
# own measured numbers are the ones that scale cleanly; it is sean.json's
# declared Strong row that breaks the pattern by repeating Jab's row
# verbatim. Combined with the byte-identical Strong=Jab oracle row already
# noted above, this interpolation result STRUCTURALLY FAVORS hypothesis
# (ii) — the sean.json data-entry-duplicate explanation — over hypothesis
# (i), a genuine engine defect (an engine bug landing exactly on the
# arithmetic midpoint of its own neighboring strengths, on all three of
# S/A/R simultaneously, would be a remarkable coincidence). This is
# external-source corroboration for the orchestrator/user follow-up
# decision above, NOT in-repo proof — the oracle file itself cannot be
# amended or PROVEN wrong from within this repo, so the arcade value stays
# asserted (from-qjson) and the divergence stays documented as xfail below,
# per the same house rule cited above. Both hypotheses remain formally open
# pending an external check against sean.json's own upstream source.
#
# RESOLVED 2026-07-11 (CAPTURE-1, arcade ground-truth capture — Fightcade
# FBNeo + Lua bridge, savestate-anchored, 2 byte-identical reps; see
# docs/arcade-frame-data/CAPTURE.md). Live arcade Dragon Smash Strong
# measures S=7/A=8/R=39 on the dp+MP whiff (f1=22734, first box=22741,
# busy->768 at 22788) — EXACT MATCH to this repo's own live measurement,
# NOT to sean.json's declared S=5/A=6/R=36 (Jab-duplicate) row. The LP and
# HP flank captures both landed exactly on sean.json's own declared rows
# (5/6/36 and 9/9/42) the same session, ruling out a rig/convention
# miscalibration explanation. Hypothesis (ii) above is now CONFIRMED:
# sean.json's own Strong row is a genuine data-entry duplicate of Jab's
# row, not a real arcade value — the ONLY oracle-side value error this
# corpus has ever confirmed against a primary hardware source (contrast
# with the STRUCTURAL/UNVERIFIABLE ERRATA.md rows, which are
# representational gaps or open ties, not confirmed-wrong numbers). The
# three `sean-dragonsmash-mp-*` entries below convert to the arcade/
# engine value as explicit literals (no longer `from-qjson`) and drop
# `xfail` — see docs/arcade-frame-data/ERRATA.md §4 (upgraded
# UNVERIFIABLE -> ORACLE-WRONG-CONFIRMED) for the full evidentiary
# writeup.
#
# ============================================================================
# SEAN TACKLE — MOTION NOT FOUND (task's own "hcf+K? verify" hypothesis
# REFUTED — hcf+K reads back as Ryuubi Kyaku instead, see below). Same
# posture as corpus-twelve.yaml's D.R.A. — MOTION NOT FOUND precedent: an
# extensive, budget-bounded attempt list, all with already-expressible
# primitives per CORPUS-AUTHORING.md Phase 5. sean.json's "Tackle (All
# Versions)" row carries real numeric data (Startup="18/22/26" per
# strength, Hit=2, Recovery=28, Throw_range=34, Parry {High:false,Low:true})
# — a genuine attack, not a zero-data placeholder — so a positive
# identification was expected but never achieved. FIFTEEN distinct
# candidate inputs tried this session, every one reading back as an
# ALREADY-NAMED move instead of a distinct Tackle dispatch:
#   1. `motion hcf LK/MK/HK` (task's own guess) -> Ryuubi Kyaku (S=25,
#      matching Ryuubi's own row exactly) for LK/MK (WHIFF at dist:250);
#      HK read HIT (connects at 250, matching Ryuubi's own reach). hcf's
#      OWN tail (DOWN, DOWN+FORWARD, FORWARD) is literally a qcf motion,
#      explaining the misread — hcf embeds qcf as a sub-sequence.
#   2. `motion 2qcf LK` -> Ryuubi Kyaku again (S=25) — 2qcf's own tail is
#      also a qcf.
#   3. `motion hcb LK` -> Tornado Kick Short (S=11, exact match) — hcb's
#      tail (DOWN, DOWN+BACK, BACK) is a qcb motion.
#   4. A manual 2qcb hold-chain (DOWN,DOWN+BACK,BACK,DOWN,DOWN+BACK,
#      BACK+LK) -> Tornado Kick Short again (S=11).
#   5. `press RIGHT; press RIGHT+LK` (fast double-tap forward + kick,
#      2-frame press / 1-frame release cadence) -> plain Short normal
#      (S=4, exact match).
#   6. `press RIGHT; press RIGHT; press RIGHT+LK` (triple-tap) -> NO FINAL
#      in window (no data — inconclusive, not investigated further).
#   7-8. `motion charge-back 45 then-forward LK` / `charge-back 90
#      then-forward LK` (both charge durations) -> plain Short normal both
#      times (S=4) — charge motions do not dispatch any Sean special via K
#      (same null result as testing charge via P for Zenten, see below).
#   9. A manual down-back-charge-then-forward hold-chain (hold DOWN+LEFT
#      45f, snap to DOWN+RIGHT then RIGHT+LK) -> plain Short again (S=4).
#   10. A manual charge-down-then-forward hold-chain (hold DOWN 45f, snap
#      to DOWN+RIGHT then RIGHT+LK) -> Ryuubi Kyaku (S=25) — the snap's own
#      DOWN,DOWN+RIGHT,RIGHT tail is a qcf.
#   11. A manual back-then-hcf hold-chain (hold LEFT 45f, then the full hcf
#      shape + LK) -> Ryuubi Kyaku again (S=25) — same qcf-tail collision.
#   12. A DP-shape hold-chain + LK (RIGHT,DOWN,DOWN+RIGHT,RIGHT+LK) ->
#      Ryuubi Kyaku (S=25) — the DP shape's own tail (DOWN+RIGHT,RIGHT) is
#      also a qcf-compatible tail.
#   13. A reverse-DP hold-chain + LK (RIGHT,DOWN,DOWN+LEFT,LEFT+LK) ->
#      Tornado Kick Short (S=11) — the reverse-DP tail is qcb-compatible.
#   14. `press LK+MK+HK` (three-kick chord) -> Far Roundhouse normal (S=7,
#      exact match) — a priority fallback to the strongest normal, not a
#      special.
# Every attempt read back as an already-named move (Ryuubi Kyaku, Tornado
# Kick Short, plain Short, plain Far Roundhouse), never as a distinct
# Tackle dispatch. Documented as OUT OF SCOPE / wants-follow-up, NO entries
# below for Tackle, no oracle edit, no compiler change — same posture as
# corpus-twelve.yaml's D.R.A. and corpus-necro.yaml's Electric Shock
# precedents. Flagged for the runbook: every already-implemented motion
# primitive (qcf/qcb/hcb/hcf/2qcf, both charge shapes, DP/reverse-DP
# hold-chains, double/triple-tap, N-button chords) was exhausted without
# success — if Tackle is ever revisited, the next step is probably to
# extend `_motion_steps` (compile_corpus.py:224-237) with a genuinely new
# shape (e.g. a raw dash-cancel primitive), which this session's budget did
# not attempt per CORPUS-AUTHORING.md Phase 5's "extend only if no
# expressible sequence lands the move" gate.
#
# ============================================================================
# ZENTEN / SEAN ROLL — CONFIRMED movement-only, Ashura posture (same
# treatment as corpus-akuma.yaml's Ashura Senkuu / corpus-dudley.yaml's
# Ducking Rush precedent). Motion CONFIRMED as `motion qcb <P>` (qcb+P) —
# `motion qcb LP`/`hcb LP` both landed the IDENTICAL move state (r1=4/
# r2=20), explained by hcb's own qcb-compatible tail (same collision
# mechanism documented in the Tackle section above) — qcb is the true
# motion, hcb merely coincides with it. `2qcf LP` was tried as a third
# candidate and instead landed Hadou Burst (the SA1 super — proj=1,
# S=2/R=61 exact match to sean.json's Hadou Burst row — out of scope,
# confirms 2qcf is NOT Roll's motion). Both charge shapes (charge-back-
# then-forward, charge-down-then-up) landed a plain Jab normal instead
# (S=3, exact match) — charge inputs do not dispatch any Sean special via
# P, consistent with the Tackle section's own charge-via-K null result.
#
# Identity confirmation: a real FINAL fires with ZERO active frames
# (first_active_raw=-1, engine_a=0, A=0) for all three strengths — the
# roll never produces a hitbox (sean.json's own Damage="-" / Parry
# {False,False} / every non-S/Hit/Recovery field "-" confirms this is a
# non-damaging dodge, not an attack). The measured total raw duration T
# EXACTLY matches the SUM of sean.json's own Startup+Hit+Recovery fields
# for each strength (Jab: T=28=2+19+7; Strong: T=38=2+29+7; Fierce:
# T=52=2+43+7) — since the zero-active-frame measurement is degenerate (no
# active window to anchor S/A/R's normal decomposition against, same
# measurement-semantics gap as Ashura Senkuu), the WHOLE duration
# classifies as pure "S" in the overlay's own broken decomposition
# (measured S=T=28/38/52, A=0, R=0 for the three strengths respectively).
# Per the Ashura Senkuu review ruling (2026-07-08, corpus-akuma.yaml:692-
# 720): outcome=WHIFF only is assertable; the oracle's own Recovery field
# is NOT independently comparable in this harness (there is no active
# anchor for a zero-active move).
# MASK-1 CORRECTION (2026-07-11): the line above used to read "R stays
# asserted via from-qjson with the measurement-semantics gap recorded in
# the xfail string (would XPASS-alert if the overlay ever gains
# zero-active R semantics)" — the user has since adopted per-entry field
# masking for ERRATA-STRUCTURAL rows (docs/frame-data-synthesis.md
# §13.15; new ERRATA §5 covers this exact zero-active class for Sean
# Roll). R is now DROPPED from expect on all three strengths (never
# substituted, never re-added) — see each entry's own FIELD-MASKED
# comment below.
#
# ============================================================================
# RYUUBI KYAKU CLASSIFICATION (flagged 2026-07-09) — task's own "possibly
# landing-clocked: ENGINE-2 candidate" hypothesis TESTED AND REFUTED. The
# ENGINE-2 signature (corpus-alex.yaml:731-761) requires the TRUE r1-edge
# (the real end of the move, after the false-cut anchor) to land EXACTLY at
# arcade's own S+A+R sum. Traced directly (Short, dist:250 HIT): the false
# anchor commits at raw offset 43 from MOVE_START (cut=1, anchor_a=10
# matching engine_a=10), but the true r1: 4->0 edge doesn't fire until raw
# offset 60 — NOT arcade's own S+A+R=25+8+15=48 (a 12-tick gap either way).
# ENGINE-2 does not apply. Direct P1.y trace confirms a genuine parabolic
# arc (y climbs 0->59 then descends back to 0, landing at raw offset 47,
# 13 ticks before the anchor commits) — "flip kick" confirmed literally
# airborne, but the landing-tick cghi flip (§13.5.1a's protected shape) is
# NOT what fires here: cghi doesn't hit its fresh-edge value (1) until
# several ticks AFTER landing, while fully grounded — i.e. the grounded
# fresh-edge gate correctly identifies this as a genuine GROUNDED early cut,
# not an airborne/landing-tick false cut.
#
# Two SEPARATE, independently-confirmed divergence clauses, established via
# a genuine dist:300 WHIFF (Ryuubi Kyaku's own reach extends to ~250-300 but
# NOT to 300 itself — confirmed unreachable-at-250, reachable-at-300... see
# entries below; dist:300 is the first distance tested that produces a true
# miss for LK/MK (HK's own WHIFF is UNREACHABLE even at dist:300 — see HK
# WHIFF UNREACHABLE below — so no true-miss distance exists for HK at all)):
#   - R-clause: item 18(b) residual grounded fresh-edge early-cut
#     (docs/frame-data-synthesis.md item 18(b) — already a generalized
#     bucket, membership includes non-UOH moves per its own table entry:
#     "ken/akuma Tatsu LK whiffs, Chun far-RH/HK/Hazan Shu, Yun Towards+MK").
#     Confirmed on the TRUE WHIFF itself (dist:300): measured R=9 vs arcade
#     15 for LK/MK — present on WHIFF, ruling out Phase 4's
#     unexplained-whiff-divergence STOP (this is the documented residual
#     bucket, not a fresh unexplained finding). HK never produces a clean
#     WHIFF (see HK WHIFF UNREACHABLE below), so HK's own R-clause evidence
#     is CONTACT-LEGS-ONLY (BLOCK/HIT), not whiff-confirmed like LK/MK's.
#   - A-clause: UNCLASSIFIED, contact-only A-overcount (flagged 2026-07-09,
#     needs follow-up classification) — the MIRROR of every #17 undercount
#     on record, same posture as corpus-ken.yaml's ken-forward-block/-hit
#     precedent. At the TRUE WHIFF (dist:300), A measures EXACTLY 8
#     (matching sean.json precisely) for LK/MK — clean (HK has no whiff
#     measurement to compare, see below). On BLOCK/HIT (dist:250,
#     dist:close), A measures 10 for all three strengths — a uniform +2
#     CONTACT-ONLY overcount. jatix/jcaix read 0 throughout this move's own
#     window (unlike genuine Universal Overhead, which engages the cg_ja
#     machinery) — this is NOT the same §13.11 cg_ja-specific mechanism as
#     the recurring UOH bucket, despite sharing UOH's own "cut=1,
#     anchor_a==engine_a" surface signature. TRACE OBSERVATION ONLY,
#     mechanism not established: HYPOTHESES ONLY, neither confirmed — (1) a
#     contact-tick declared-credit inflation distinct from the jatix-gated
#     UOH mechanism, or (2) a double-credit re-entry on the contact path
#     (same candidate hypothesis shape as ken-forward-block's own writeup).
#     Flagged to orchestrator for follow-up classification.
#   - HK WHIFF UNREACHABLE (correction, 2026-07-09 review fix): unlike LK/MK,
#     Ryuubi Kyaku RH's own WHIFF is UNREACHABLE even at dist:300 (DIST_MAX,
#     compile_corpus.py:95) — HK connects (outcome=HIT) at every distance
#     tested, including the compile-time maximum, same traveling-move
#     harness-gap bucket as this file's own TORNADO KICK — Forward/RH
#     WHIFF-unreachable precedent (see file header TORNADO KICK section
#     above; docs/frame-data-synthesis.md §12.1). A declared-WHIFF entry for
#     HK at dist:300 was originally authored and shipped in this corpus; it
#     measured outcome=HIT S=25/A=8/R=9 (a declared-WHIFF that HITs), so the
#     entry has been REMOVED rather than asserting a WHIFF this harness
#     cannot produce for this strength. HK's own item-18(b) R-clause and
#     A-clause evidence above is therefore CONTACT-LEGS-ONLY (BLOCK/HIT) — a
#     clean whiff for HK was never observed, unlike LK/MK.
#
# CORRECTED 2026-07-09 (§13.5.1b guard-rearm commit gate, lever H): the
# "ENGINE-2 does not apply" conclusion above was reached via a NAIVE test
# (does the true r1 edge land exactly on arcade's own S+A+R sum?) that the
# eventual §13.5.1b diagnosis itself falsified as a general discriminator
# (docs/frame-data-synthesis.md §13.5.1b candidate (i): a bare "lands
# exactly on the arcade sum" test does not separate real cuts from false
# ones). The REAL discriminator is the engine's own guard_flag: this move's
# post-anchor tail keeps guard_flag=3 for the whole tail (never re-arming
# before the natural r1 edge) exactly like every other lever-H false cut,
# so the R-clause above WAS the false-cut mechanism after all, despite
# failing the naive sum test. The guard-rearm gate now correctly refuses
# the commit: LK/MK whiff legs resolve to arcade-exact R=15 (now plain
# PASS below); LK/MK/HK-hit contact legs also resolve R to arcade-exact 15
# but keep the separate, untouched A-overcount clause (§12.2.3 candidate).
# HK-BLOCK is the one exception: its own pre-fix R was 8 (a distinct,
# 1-tick-shorter measurement than the other five contact legs' R=9, already
# flagged above) and only extends to R=14 post-fix - still 1 tick short of
# arcade 15, a residual this fix does not explain (kept as its own +1
# R-variance clause, stacked on top of the unrelated A-overcount; its adv
# shifts correspondingly, -2 measured vs -3 arcade, and stays asserted at
# the true arcade value). See docs/frame-data-synthesis.md §13.5.1b.
#
# ============================================================================
# TORNADO KICK — fully clean (S/A/R/adv exact on every reachable outcome,
# all three strengths). Forward and RH's own WHIFF is UNREACHABLE even at
# dist:300 (DIST_MAX, compile_corpus.py:95) — both strengths connect
# (outcome=HIT) at every distance tested including the compile-time
# maximum, byte-identical numbers at dist:250 and dist:300 — a traveling
# kick with reach exceeding this harness's scriptable range, same known
# harness-gap bucket as Ryu's own Hadouken WHIFF-unreachable precedent
# (docs/frame-data-synthesis.md §12.1). Short's own WHIFF IS reachable
# (dist:250) and matches sean.json exactly, confirming the calibration
# surface holds; Forward/RH's own BLOCK+HIT entries below stand in for
# their unreachable WHIFF per this bucket's own established convention.
#
# ============================================================================
# DRAGON SMASH JAB/FIERCE — fully clean (see NEW FINDING: DRAGON SMASH
# STRONG above for the isolated Strong-only divergence).
#
# ============================================================================
# ISSUE #17 CENSUS (lever-F-tested on a scratch build,
# fd_restore_revoked_declared_credit forced 0, reverted before commit,
# same-binary protocol):
#   - UNCHANGED by the lever-F toggle (shape (a1), unrecoverable per
#     §13.11 — stay xfail): sean-farstrong-block (A=1 vs arcade 4, S/R/adv
#     exact), sean-crforward-block (A=1 vs arcade 5, S/R/adv exact),
#     sean-farroundhouse-block/-hit (A=2 vs arcade 4, STACKED with a small
#     R-undershoot — see below), sean-twdshp-block (A=5 vs arcade 7,
#     STACKED with a larger R-undershoot — see below).
#   - Far Roundhouse's own #17 undercount is STACKED with a small (1-frame)
#     R-undershoot present on BOTH BLOCK and HIT alike (R=25 vs the clean
#     WHIFF's own R=26) — NOT the classic HIT-only Class-1 shape (which
#     would leave BLOCK clean); recorded as an unclassified trace
#     observation, not independently bucketed (small magnitude, adv stays
#     exact on both outcomes: Badv=-9, Hadv=-7, both matching sean.json).
#   - Towards + Fierce's own #17 undercount is STACKED with a LARGER
#     (3-frame) R-undershoot, BLOCK-only (this move has no HIT entry per
#     the non-KD Towards-normal convention below) — R=22 vs the clean
#     WHIFF's own R=25; adv stays exact (Badv=-6, matching sean.json).
#     Same unclassified-companion treatment as Far Roundhouse above.
#   - UNAFFECTED either way (clean, no revoke ever fires): the entire
#     six-button normals ladder (Jab/Strong/Fierce/Short/Forward/
#     Roundhouse, every BLOCK/HIT/WHIFF entry), Far Fierce (BLOCK/WHIFF),
#     every other crouching normal (Jab/Strong/Fierce/Short/Roundhouse),
#     Towards + Roundhouse (BLOCK/WHIFF/HIT), Tornado Kick (all three
#     strengths, all outcomes), Throw, UOH's own A-clause (all three
#     outcomes exact).
#   UPDATE (2026-07-10, §13.13): sean-farstrong-block and
#   sean-crforward-block (single-clause, unhedged) convert to a plain PASS
#   asserting the measured A citing §13.13. sean-farroundhouse-block/-hit
#   and sean-twdshp-block narrow to their stacked R-undershoot clause only
#   (still UNCLASSIFIED, unchanged) - the A-clause now asserts the
#   measured value citing §13.13 on all three.
#   SWEEP-2 UPDATE (2026-07-10, classification sweep #2 fable-grade audit):
#   the "stacked, unclassified R-undershoot" clauses named above CLOSE
#   EXACTLY under the window's measured counting rule and are now F2
#   proper (§12.2.2) FULL CONVERTS, xfail REMOVED entirely:
#   sean-farroundhouse-block/-hit (R->25, contact-only cell 24(6) vs
#   whiff-only cell 16(7 R-share), shared 32..52=19, -1 exact) and
#   sean-twdshp-block (R->22, contact-only cell 56(1) vs whiff-only cell
#   48(4), shared 60..72=21, -3 exact). All per-tick recounts P2 direct,
#   run-sean this session.
#
# ============================================================================
# UOH — mostly clean, two SEPARATE small divergences (NOT the recurring
# Alex/Elena/Necro three-way compound bucket — A is exact everywhere here).
# sean.json: S=15/A=8/R=7, Badv/Hadv both literal "-" (no expect.adv).
# BLOCK is FULLY clean (S=15/A=8/R=7 exact, matching sean.json on every
# field) — the anchor tick (cut=1, anchor_a=8=engine_a=8) fires but the
# displayed numbers land exactly on the oracle regardless.
#   - WHIFF: item 18(b) residual grounded fresh-edge early-cut (same
#     generalized bucket cited above for Ryuubi Kyaku): measured R=6 vs
#     arcade 7 (a small 1-frame undershoot, present on WHIFF itself). S/A
#     exact (S=15, A=8).
#   - HIT: §13.10 Class 1 hit-branch recovery-chain divergence (the
#     attacker's own genuinely shorter parallel recovery chain on HIT, cf.
#     the runbook's Class-1 bucket): measured R=5 vs BLOCK's own clean R=7
#     (a 2-frame undershoot, HIT-only — BLOCK stays exact, ruling out a
#     WHIFF-style early-cut explanation for this leg). S/A exact.
#
# ============================================================================
# TOWARDS + ROUNDHOUSE — ENGINE-3 STRIKE-KD PREDICATE GAP — RESOLVED
# 2026-07-09 (§13.6.2b, tools/frame-data lever I). sean.json's
# Hit_advantage="D" (a knockdown row); the HIT leg now measures kd=1,
# matching arcade. Trace-decoded: at the attacker-recovery truncation
# tick (the last-raw[]-cell sample point) the defender reads
# P2.r1=1/P2.r2=17 — squarely inside the measured down-family set
# `{14..23} ∪ {27,28}` (`fd_r2_is_down_family()`, frame_data_overlay.c).
# This move's flip is via the widened last-cell set alone (lever E's
# anchor); no latch-only member is needed here, unlike urien-vkd-lk-hit
# (see docs/frame-data-synthesis.md §13.6.2b). S/A/R/adv frame-data
# fields are all exact on every outcome of this move; kd is now a plain
# assertion (xfail removed — this is one of the two entries this fix
# flips to a full PASS, the other being corpus-remy.yaml's
# remy-crfierce-hit).
#
# adv NOTE: this move's measured adv=+12 is NOT an outlier among this
# repo's "D"-row knockdowns — remy-crroundhouse-hit (corpus-remy.yaml)
# measures the IDENTICAL +12 on its own "D"-row Crouching Roundhouse.
# Other "D"-row HIT entries span a much wider range, not a tight
# +90..+125 band: alex-crroundhouse-hit +28, urien-crroundhouse-hit +33,
# yang-crroundhouse-hit +36 / oro-crroundhouse-hit +37 /
# yun-crroundhouse-hit +38 (the Crouching Roundhouse family clusters
# together across the cast), up to alex-crfierce-hit +85 /
# yun-twdshp-hit +85 at the high end. Candidate mechanism (per the
# ENGINE-3 review pass, static-code-grounded, not independently re-traced
# this session): r2=17 dispatches to `Damage_17000` (`plpdm.c:613-666`),
# whose case 2 branch contains a `DIP_AUTO_AIR_RECOVERY_DISABLED`
# auto-recovery check (`plpdm.c:651-656`) — once the launched defender's
# downward velocity exceeds a threshold (`mvxy.a[1].real.h < -2`), the
# engine transitions the defender straight back to ordinary
# `routine_no[1]=0` (idle, not the down family) and re-dispatches into
# `routine_no[2]=23`. An early, automatic air recovery of this kind would
# end the knockdown well before a "full" hard-knockdown wakeup, which
# could explain a modest advantage figure without this being any less of
# a genuine, arcade-confirmed knockdown — offered as a hedged candidate,
# not a confirmed explanation for this specific +12 value (which, per the
# remy precedent above, may simply be this move's own ordinary figure
# rather than needing a special explanation at all).
#
# ============================================================================
# THROW — §13.10 CLASS 3, NON-SATURATED connected grab (T=41, NOT the
# FD_METER_LEN-1=71 saturation Hugo/Ken/Twelve's own grabs hit) — same
# mechanism and IDENTICAL numbers to corpus-ryu.yaml's own ryu-throw-hit
# (`endrel=1` fires, R = meter_len - S - A = 41-2-1 = 38, T=41 self-
# consistent). sean.json has two throw-family rows ("Throw" and "Back +
# Throw"), byte-identical on S/A/R/Badv/Hadv (only Damage/Stun differ,
# 21/10 vs 24/4) — "Throw" used per the ryu/q/chunli/remy/necro/twelve
# bare-"Throw" convention. dist:20 used for the HIT leg (sean.json's own
# Throw_range=20 field, well inside the ken-throw-hit dist:close trap
# radius lesson). kd=1 measured directly; adv intentionally unasserted
# (Hit_advantage="D", literal, same convention as every other saturated/
# non-saturated grab in this repo).
#
# ============================================================================
character: sean
oracle: sean.json
inter_entry_wait: 200
entries:

# --- Standing normals, BLOCK (dummy: stand) --------------------------------

- label: sean-jab-block
  setup: { dist: close, dummy: stand }
  input: "press LP"
  qjson: { name: "Jab" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-strong-block
  setup: { dist: close, dummy: stand }
  input: "press MP"
  qjson: { name: "Strong" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

# Far Strong: dist:100 connects (matches ken.json/corpus-ryu.yaml's own
# dist:100 Far Strong convention exactly); issue #17 shape (a1),
# lever-F-tested UNCHANGED (see file header ISSUE #17 CENSUS).
# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=1 is the
# engine-truth credit actually accrued on the BLOCK path - issue #17 shape (a1):
# lever-F-tested (fd_restore_revoked_declared_credit forced 0 on a scratch
# build, reverted before commit) and confirmed UNCHANGED - no revoke fires.
# Arcade (whiff-canonical, from-qjson) A=4 - preserved here per the
# convention. S/R/adv remain arcade-exact (from-qjson).
- label: sean-farstrong-block
  setup: { dist: 100, dummy: stand }
  input: "press MP"
  qjson: { name: "Far Strong" }
  outcome: BLOCK
  expect: { S: from-qjson, A: 1, R: from-qjson, adv: from-qjson }

- label: sean-fierce-block
  setup: { dist: close, dummy: stand }
  input: "press HP"
  qjson: { name: "Fierce" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

# Far Fierce: dist:100 connects, fully clean (no divergence found on any
# field, matching sean.json exactly).
- label: sean-farfierce-block
  setup: { dist: 100, dummy: stand }
  input: "press HP"
  qjson: { name: "Far Fierce" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-short-block
  setup: { dist: close, dummy: stand }
  input: "press LK"
  qjson: { name: "Short" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-forward-block
  setup: { dist: close, dummy: stand }
  input: "press MK"
  qjson: { name: "Forward" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-roundhouse-block
  setup: { dist: close, dummy: stand }
  input: "press HK"
  qjson: { name: "Roundhouse" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

# Far Roundhouse: dist:100 connects; issue #17 shape (a1) STACKED with a
# small (1-frame), unclassified R-undershoot present on BOTH BLOCK and HIT
# (see file header). Lever-F-tested UNCHANGED.
# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=2 is the
# engine-truth credit actually accrued on the BLOCK path - issue #17 shape
# (a1), lever-F-tested UNCHANGED. Arcade (whiff-canonical, from-qjson) A=4 -
# preserved here per the convention.
#
# SWEEP-2 (2026-07-10) FULL CONVERT: R=25 also now asserts the engine-truth
# value, xfail REMOVED. F2 proper (§12.2.2), classification sweep #2
# fable-grade audit: R-window closure exact - contact-only cell 24(6) vs
# whiff-only cell 16(7 R-share), shared cells 32..52=19; -1 exact (P2 direct,
# run-sean). Byte-identical on HIT. Arcade (whiff-canonical) R=26 -
# superseded per the convention. S/adv remain arcade-exact (from-qjson).
- label: sean-farroundhouse-block
  setup: { dist: 100, dummy: stand }
  input: "press HK"
  qjson: { name: "Far Roundhouse" }
  outcome: BLOCK
  expect: { S: from-qjson, A: 2, R: 25, adv: from-qjson }

# --- HIT sample per button family (dummy: none, close range) ---------------

- label: sean-jab-hit
  setup: { dist: close, dummy: none }
  input: "press LP"
  qjson: { name: "Jab" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-strong-hit
  setup: { dist: close, dummy: none }
  input: "press MP"
  qjson: { name: "Strong" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-fierce-hit
  setup: { dist: close, dummy: none }
  input: "press HP"
  qjson: { name: "Fierce" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-short-hit
  setup: { dist: close, dummy: none }
  input: "press LK"
  qjson: { name: "Short" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-forward-hit
  setup: { dist: close, dummy: none }
  input: "press MK"
  qjson: { name: "Forward" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-roundhouse-hit
  setup: { dist: close, dummy: none }
  input: "press HK"
  qjson: { name: "Roundhouse" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

# Same stacked shape as sean-farroundhouse-block (see file header).
# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=2 is the
# engine-truth credit actually accrued on the HIT path - same shape (a1)
# finding as sean-farroundhouse-block. Arcade (whiff-canonical, from-qjson)
# A=4 - preserved here per the convention.
#
# SWEEP-2 (2026-07-10) FULL CONVERT: R=25 also now asserts the engine-truth
# value, xfail REMOVED - byte-identical branch to sean-farroundhouse-block
# (same -1 exact closure). Arcade (whiff-canonical) R=26 - superseded per the
# convention. S/adv remain arcade-exact (from-qjson).
- label: sean-farroundhouse-hit
  setup: { dist: 100, dummy: none }
  input: "press HK"
  qjson: { name: "Far Roundhouse" }
  outcome: HIT
  expect: { S: from-qjson, A: 2, R: 25, adv: from-qjson }

# --- WHIFF sample per button family (dummy: none, dist 250) -----------------
# sean.json has a Far row for Strong, Fierce, AND Roundhouse (unlike
# Twelve's Strong-only Far row) — WHIFF below cites each button's own Far
# row where one exists. All six match arcade exactly, confirming the
# calibration surface holds (CORPUS-AUTHORING.md Phase 4), including for
# Far Strong/Far Roundhouse whose close-range BLOCK/HIT entries above carry
# their own divergences.

- label: sean-lp-whiff
  setup: { dist: 250, dummy: none }
  input: "press LP"
  qjson: { name: "Jab" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-mp-whiff
  setup: { dist: 250, dummy: none }
  input: "press MP"
  qjson: { name: "Far Strong" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-hp-whiff
  setup: { dist: 250, dummy: none }
  input: "press HP"
  qjson: { name: "Far Fierce" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-lk-whiff
  setup: { dist: 250, dummy: none }
  input: "press LK"
  qjson: { name: "Short" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-mk-whiff
  setup: { dist: 250, dummy: none }
  input: "press MK"
  qjson: { name: "Forward" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-hk-whiff
  setup: { dist: 250, dummy: none }
  input: "press HK"
  qjson: { name: "Far Roundhouse" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# --- Crouching normals, BLOCK (dummy: stand) --------------------------------

- label: sean-crjab-block
  setup: { dist: close, dummy: stand }
  input: "press DOWN+LP"
  qjson: { name: "Crouching Jab" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-crstrong-block
  setup: { dist: close, dummy: stand }
  input: "press DOWN+MP"
  qjson: { name: "Crouching Strong" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-crfierce-block
  setup: { dist: close, dummy: stand }
  input: "press DOWN+HP"
  qjson: { name: "Crouching Fierce" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-crshort-block
  setup: { dist: close, dummy: stand }
  input: "press DOWN+LK"
  qjson: { name: "Crouching Short" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

# Issue #17 shape (a1), lever-F-tested UNCHANGED (see file header). WHIFF
# added below (Phase 4/5 discipline for a divergent move).
# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=1 is the
# engine-truth credit actually accrued on the BLOCK path - issue #17 shape (a1):
# lever-F-tested (fd_restore_revoked_declared_credit forced 0, reverted
# before commit) and confirmed UNCHANGED - no revoke fires. Arcade (whiff-
# canonical, from-qjson) A=5 - preserved here per the convention. S/R/adv
# remain arcade-exact (from-qjson).
- label: sean-crforward-block
  setup: { dist: close, dummy: stand }
  input: "press DOWN+MK"
  qjson: { name: "Crouching Forward" }
  outcome: BLOCK
  expect: { S: from-qjson, A: 1, R: from-qjson, adv: from-qjson }

- label: sean-crforward-whiff
  setup: { dist: 250, dummy: none }
  input: "press DOWN+MK"
  qjson: { name: "Crouching Forward" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# Crouching Roundhouse is a KD row (sean.json Hit_advantage="D") - gets
# WHIFF+HIT in addition to BLOCK, per Phase 4's mandatory whiff-vs-table
# rule. Fully clean on every outcome.
- label: sean-crroundhouse-block
  setup: { dist: close, dummy: stand }
  input: "press DOWN+HK"
  qjson: { name: "Crouching Roundhouse" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-crroundhouse-whiff
  setup: { dist: 250, dummy: none }
  input: "press DOWN+HK"
  qjson: { name: "Crouching Roundhouse" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-crroundhouse-hit
  setup: { dist: close, dummy: none }
  input: "press DOWN+HK; wait 60"
  qjson: { name: "Crouching Roundhouse" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, kd: 1 }

# --- Command normals (Towards + button), BLOCK + WHIFF (+HIT for the KD
# row) -----------------------------------------------------------------
# sean.json has exactly TWO Towards-prefixed rows — "Towards + Fierce" and
# "Towards + Roundhouse" — verified against every Name string in the
# fetched oracle. Towards + Fierce is non-KD (Hit_advantage=-4, numeric) —
# BLOCK+WHIFF only, per the Ken/Dudley/Makoto/Urien/Akuma/Oro/Alex/Elena/
# Necro/Twelve non-D-row precedent. Towards + Roundhouse IS a KD row
# (Hit_advantage="D") — gets BLOCK+WHIFF+HIT per Phase 4.

# Issue #17 shape (a1) STACKED with a LARGER (3-frame), unclassified
# R-undershoot (see file header ISSUE #17 CENSUS). Lever-F-tested
# UNCHANGED.
# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=5 is the
# engine-truth credit actually accrued on the BLOCK path - issue #17 shape
# (a1), lever-F-tested UNCHANGED. Arcade (whiff-canonical, from-qjson) A=7 -
# preserved here per the convention.
#
# SWEEP-2 (2026-07-10) FULL CONVERT: R=22 also now asserts the engine-truth
# value, xfail REMOVED. F2 proper (§12.2.2), classification sweep #2
# fable-grade audit: R-window closure exact - contact-only cell 56(1) vs
# whiff-only cell 48(4), shared cells 60..72=21; -3 exact (P2 direct,
# run-sean). Arcade (whiff-canonical) R=25 - superseded per the convention.
# S/adv remain arcade-exact (from-qjson).
- label: sean-twdshp-block
  setup: { dist: close, dummy: stand }
  input: "press RIGHT+HP"
  qjson: { name: "Towards + Fierce" }
  outcome: BLOCK
  expect: { S: from-qjson, A: 5, R: 22, adv: from-qjson }

- label: sean-twdshp-whiff
  setup: { dist: 250, dummy: none }
  input: "press RIGHT+HP"
  qjson: { name: "Towards + Fierce" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-twdshk-block
  setup: { dist: close, dummy: stand }
  input: "press RIGHT+HK"
  qjson: { name: "Towards + Roundhouse" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-twdshk-whiff
  setup: { dist: 250, dummy: none }
  input: "press RIGHT+HK"
  qjson: { name: "Towards + Roundhouse" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# ENGINE-3 strike-KD predicate gap — RESOLVED 2026-07-09 (§13.6.2b; see
# file header for the full discussion incl. the DIP_AUTO_AIR_RECOVERY_
# DISABLED note on this move's adv=+12, which is not an outlier — see
# file header). kd now measures 1, matching sean.json's
# Hit_advantage="D". S/A/R exact (S=15, A=5, R=18).
- label: sean-twdshk-hit
  setup: { dist: close, dummy: none }
  input: "press RIGHT+HK; wait 60"
  qjson: { name: "Towards + Roundhouse" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, kd: 1 }

# --- UOH (press MP+MK) ------------------------------------------------------
# See file header UOH block: BLOCK is fully clean; WHIFF carries a small
# item-18(b) R-undershoot; HIT carries a separate, small §13.10 Class-1
# R-undershoot. NOT the recurring Alex/Elena/Necro/Twelve three-way
# A-diverges-too compound — A is exact on every outcome here.
# SECTION ACCOUNTING (Phase 7): 2/3 entries carry a divergence (67% xfail;
# BLOCK itself is fully clean) — crosses the >50%-of-section threshold.
# GRANTED by orchestrator at commit 2026-07-09 (review-verified post-fix: UOH/Roll routine buckets; Ryuubi item-18(b) LK/MK-whiff-verified + hedged UNCLASSIFIED A-overcount; Dragon Smash Strong oracle-duplicate question on the user-review list w/ the interpolation finding). User review pending (overnight autonomous run) (orchestrator grants at commit; both clauses are small,
# 1-2 frame members of already-established buckets — item 18(b) and §13.10
# Class 1 — not new mechanisms, so the grant should be routine, but is not
# self-issued here).
# UPDATE (2026-07-10, §13.10 HIT-R convention, F2b compliance): NOW 1/3
# xfail (33%, below threshold) - sean-uoh-hit converts to a plain PASS
# asserting the measured R citing §13.10 directly (compliance, not new
# §13.13 scope). sean-uoh-whiff's item-18(b) R clause is unaffected.

# §13.17 RE-ANCHOR-1 (2026-07-11): item-18 CLOSED for this row — lever N
# now measures R=7, matching arcade/oracle exactly. Previously a
# genuinely-missed WHIFF-outcome XFAIL (found by the census, not the
# plan's first draft — docs/plan-frame-data-completion.md OVERLAY
# RE-ANCHOR §5). Prior xfail evidence, RELOCATED not deleted: "item 18(b)
# grounded-cleanup whiff-R-truncation (docs/frame-data-synthesis.md item
# 18(b), same generalized bucket as ken/akuma Tatsu LK whiffs / Chun
# far-RH-HK-Hazan Shu / Yun Towards+MK — see file header): measured R=6
# vs arcade 7 (a small 1-frame undershoot). S/A exact (S=15, A=8); cut=1,
# anchor_a=8 matching engine_a=8."
- label: sean-uoh-whiff
  setup: { dist: 250, dummy: none }
  input: "press MP+MK"
  qjson: { name: "Universal Overhead" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# CORRECTED 2026-07-13 (UOH-CLOSURE lever T, docs/frame-data-synthesis.md
# §13.20 SESSION 11 branch A; <sp>/zero/arcade-s11/session-report.md):
# from-qjson (R=7, whiff-canonical) was a coincidental match - session 8's
# original BLOCK capture at this value was retroactively proven a WHIFF
# (dummy walked back out of range pre-contact; whiff-shaped raw-box-length
# + zero-freeze signature, §13.20 §2.2). Session 11 re-captured this leg
# with the dummy cornered (walkback impossible) and a mandatory in-tape
# contact witness (attacker frz 8->0 overlapping a freeze-extended 18-raw
# box window, vs the 8-raw whiff-canonical signature): real arcade R=8,
# matching this engine's own busy-edge/lever-R construction exactly (lever
# T). Re-literaled to the arcade value; probe-confirmed actionable at fc
# 262429, zero early acceptance.
- label: sean-uoh-block
  setup: { dist: close, dummy: stand }
  input: "press MP+MK"
  qjson: { name: "Universal Overhead" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: 8 }

# §13.10 HIT-R convention (adopted 2026-07-10, §13.10-compliance conversion,
# F2b): R=5 is the engine-truth recovery actually played on the HIT path -
# attacker's own genuinely shorter recovery chain on HIT (BLOCK stays exact,
# ruling out a WHIFF-style early-cut explanation for this leg); cut=1,
# anchor_a=8 matching engine_a=8. Arcade (whiff-canonical) R=7 - preserved
# here per the convention. S/A remain arcade-exact (from-qjson).
#
# CORRECTED 2026-07-13 (UOH-CLOSURE lever T, docs/frame-data-synthesis.md
# §13.20; <sp>/zero/arcade-s8/session-report.md HIT legs, valid contact:
# frz=8 in-box, p1life 155): the §13.10 F2b convention value (5) is
# superseded - real arcade measures R=6, matching this engine's own
# busy-edge/lever-R construction exactly. Re-literaled to the arcade
# value.
- label: sean-uoh-hit
  setup: { dist: close, dummy: none }
  input: "press MP+MK"
  qjson: { name: "Universal Overhead" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: 6 }

# --- Throw (LP+LK) -----------------------------------------------------------
# See file header THROW — §13.10 CLASS 3, NON-SATURATED.

- label: sean-throw-whiff
  setup: { dist: 250, dummy: none }
  input: "press LP+LK"
  qjson: { name: "Throw" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-throw-hit
  setup: { dist: 20, dummy: none }
  input: "press LP+LK; wait 90"
  qjson: { name: "Throw" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: 38, kd: 1 }

# --- Ground specials probe (ONE session budget) -----------------------------
# See file header for the full MOTION VERIFICATION / SEAN TACKLE — MOTION
# NOT FOUND / ZENTEN-SEAN ROLL / RYUUBI KYAKU CLASSIFICATION / TORNADO KICK
# / DRAGON SMASH STRONG writeups. Ordering below places Dragon Smash (no
# residue) before Tornado Kick/Ryuubi Kyaku (both traveling, no residue)
# before Sean Roll (movement-only, no residue) — none of these leave
# grab-style residue, so strict §12.1 grab-avoidance ordering does not
# apply here; Throw above is already the corpus's own last residue-leaving
# move.

# --- Dragon Smash (DP-shape hold-chain), all 3 strengths --------------------
# See file header NEW FINDING: DRAGON SMASH STRONG (flagged to
# orchestrator/user for follow-up — Jab/Fierce are both fully clean, Strong
# alone diverges uniformly on WHIFF/BLOCK/HIT). See also the file header's
# DECISIVE INTERPOLATION FINDING addendum — measured Strong interpolates
# exactly between Jab and Fierce, structurally favoring the sean.json
# data-entry-duplicate hypothesis (not proven in-repo; arcade value stays
# asserted).
# SECTION ACCOUNTING (Phase 7): 3/9 entries (all three Strong legs) carry
# a divergence (33% xfail, under the 50% threshold) — no sign-off gate
# tripped, but flagged anyway given Phase 4's STOP-rule language (see file
# header for the full escalation reasoning). GRANTED by orchestrator at commit 2026-07-09 (review-verified post-fix: UOH/Roll routine buckets; Ryuubi item-18(b) LK/MK-whiff-verified + hedged UNCLASSIFIED A-overcount; Dragon Smash Strong oracle-duplicate question on the user-review list w/ the interpolation finding). User review pending (overnight autonomous run) (orchestrator
# grants at commit).
#
# RESOLVED 2026-07-11 (CAPTURE-1): the Dragon Smash Strong user-review item
# is CLOSED. Arcade ground-truth capture (docs/arcade-frame-data/CAPTURE.md)
# confirms sean.json's declared Strong row is a data-entry duplicate of
# Jab's row; live arcade plays S=7/A=8/R=39, matching this repo's own
# measurement exactly. All three `sean-dragonsmash-mp-*` entries below now
# assert the arcade-confirmed value as an explicit literal and are plain
# PASS (see file header RESOLVED note + ERRATA.md §4).

- label: sean-dragonsmash-lp-whiff
  setup: { dist: 250, dummy: none }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+LP 2; wait 1"
  qjson: { name: "Dragon Smash (Jab)" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-dragonsmash-lp-block
  setup: { dist: close, dummy: stand }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+LP 2; wait 1"
  qjson: { name: "Dragon Smash (Jab)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-dragonsmash-lp-hit
  setup: { dist: close, dummy: none }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+LP 2; wait 1"
  qjson: { name: "Dragon Smash (Jab)" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-dragonsmash-mp-whiff
  setup: { dist: 250, dummy: none }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+MP 2; wait 1"
  qjson: { name: "Dragon Smash (Strong)" }
  outcome: WHIFF
  expect: { S: 7, A: 8, R: 39 }
  # RESOLVED 2026-07-11 (CAPTURE-1): arcade ground-truth capture confirms
  # this repo's own live measurement (S=7/A=8/R=39), NOT sean.json's
  # declared S=5/A=6/R=36 (a Jab-row duplicate, ERRATA.md §4). Literal
  # asserted, not from-qjson, per the oracle-error-confirmed exception
  # (feedback-no-shipping-wrong-data.md's one sanctioned carve-out). See
  # docs/arcade-frame-data/CAPTURE.md.

- label: sean-dragonsmash-mp-block
  setup: { dist: close, dummy: stand }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+MP 2; wait 1"
  qjson: { name: "Dragon Smash (Strong)" }
  outcome: BLOCK
  expect: { S: 7, A: 8, R: 39, adv: -30 }
  # RESOLVED 2026-07-11 (CAPTURE-1): same arcade-confirmed value as
  # sean-dragonsmash-mp-whiff (see that entry + file header RESOLVED
  # note). adv=-30 is the same real recovery, arithmetically derived, not
  # separately arcade-captured (the rig's frame log has no adv field).

- label: sean-dragonsmash-mp-hit
  setup: { dist: close, dummy: none }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+MP 2; wait 1"
  qjson: { name: "Dragon Smash (Strong)" }
  outcome: HIT
  expect: { S: 7, A: 8, R: 39 }
  # RESOLVED 2026-07-11 (CAPTURE-1): same arcade-confirmed value as
  # sean-dragonsmash-mp-whiff (see that entry + file header RESOLVED
  # note).

- label: sean-dragonsmash-hp-whiff
  setup: { dist: 250, dummy: none }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+HP 2; wait 1"
  qjson: { name: "Dragon Smash (Fierce)" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-dragonsmash-hp-block
  setup: { dist: close, dummy: stand }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+HP 2; wait 1"
  qjson: { name: "Dragon Smash (Fierce)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-dragonsmash-hp-hit
  setup: { dist: close, dummy: none }
  input: "hold RIGHT 2; hold DOWN 2; hold DOWN+RIGHT 2; hold RIGHT+HP 2; wait 1"
  qjson: { name: "Dragon Smash (Fierce)" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# --- Tornado Kick (motion qcb + K), all 3 strengths -------------------------
# See file header TORNADO KICK — fully clean. Forward/RH's own WHIFF is
# UNREACHABLE even at dist:300 (DIST_MAX) — both always connect; their own
# BLOCK+HIT entries stand in for the missing WHIFF per the established
# fireball-WHIFF-unreachable harness-gap bucket.

- label: sean-tornado-lk-whiff
  setup: { dist: 250, dummy: none }
  input: "motion qcb LK"
  qjson: { name: "Tornado Kick (Short)" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-tornado-lk-block
  setup: { dist: close, dummy: stand }
  input: "motion qcb LK"
  qjson: { name: "Tornado Kick (Short)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-tornado-lk-hit
  setup: { dist: close, dummy: none }
  input: "motion qcb LK"
  qjson: { name: "Tornado Kick (Short)" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# Forward: WHIFF unreachable (connects at dist:250 AND dist:300, byte-
# identical numbers both distances) — BLOCK+HIT only, per the harness-gap
# bucket cited in the file header.
- label: sean-tornado-mk-block
  setup: { dist: close, dummy: stand }
  input: "motion qcb MK"
  qjson: { name: "Tornado Kick (Forward)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-tornado-mk-hit
  setup: { dist: 250, dummy: none }
  input: "motion qcb MK"
  qjson: { name: "Tornado Kick (Forward)" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# RH: same WHIFF-unreachable shape as Forward (connects at dist:250 AND
# dist:300).
- label: sean-tornado-hk-block
  setup: { dist: close, dummy: stand }
  input: "motion qcb HK"
  qjson: { name: "Tornado Kick (RH)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: from-qjson, R: from-qjson, adv: from-qjson }

- label: sean-tornado-hk-hit
  setup: { dist: 250, dummy: none }
  input: "motion qcb HK"
  qjson: { name: "Tornado Kick (RH)" }
  outcome: HIT
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# --- Ryuubi Kyaku (motion qcf + K), all 3 strengths -------------------------
# See file header RYUUBI KYAKU CLASSIFICATION. dist:300 is the first
# distance tested that produces a genuine miss for LK/MK (dist:250 connects
# for all three). HK's own WHIFF is UNREACHABLE even at dist:300 — see the
# section HK WHIFF UNREACHABLE note below (same file-header bucket, cited
# there against TORNADO KICK's Forward/RH WHIFF-unreachable precedent) — so
# NO dist produces a genuine miss for HK; only LK/MK have a whiff-side
# measurement.
#
# ORDERING NOTE (§12.1 row 2, teleport clamp after connecting dash
# specials — the corpus-q.yaml/corpus-yun.yaml precedent, hit live during
# THIS corpus's own authoring): both distance-sensitive dist:300 WHIFF
# entries (LK, MK) are ordered FIRST, before any connecting BLOCK/HIT entry.
# In the first authoring run (whiff/block/hit interleaved per strength), an
# attempted HK dist:300 WHIFF teleport clamped to an ACTUAL dist=265
# (MOVE_START atk_x=424 def_x=689) after sean-ryuubi-mk-hit's connect, and
# Ryuubi HK still connected at 265 — at the time read as a corrupted HIT
# reading caused by ordering/residue. The reorder resolves that clamp
# artifact for LK/MK (both now whiff cleanly at the declared dist:300,
# unaffected by prior entries). It does NOT resolve HK: even reordered,
# with whiffs leaving no residue ahead of it and no teleport-clamp
# contamination possible, HK's own dist:300 STILL measured outcome=HIT (see
# HK WHIFF UNREACHABLE below) — proving the original dist=265 clamp
# reading was never the true cause. HK simply has no reachable WHIFF
# distance at all; the declared-WHIFF entry for it has been REMOVED rather
# than shipping a WHIFF that HITs.
#
# HK CONTACT DISTANCE: dist:close (56px) does NOT connect for the HK
# strength — the flip kick overshoots a point-blank dummy (verified live in
# the first authoring run: MOVE_START dist=49/54, both legs read a genuine
# WHIFF S=25/A=8/R=9 — an overshoot miss, not label bleed). HK's contact
# legs use dist:250 (probe-verified connecting distance; LK/MK connect at
# close normally and keep it).
#
# HK WHIFF UNREACHABLE (correction, 2026-07-09 review fix): Ryuubi Kyaku RH
# connects (outcome=HIT) at every distance tested, including dist:300
# (DIST_MAX, compile_corpus.py:95) — same traveling-move harness-gap bucket
# as this file's own Tornado Kick Forward/RH WHIFF-unreachable precedent
# (file header TORNADO KICK section; docs/frame-data-synthesis.md §12.1). A
# sean-ryuubi-hk-whiff entry declaring outcome:WHIFF at dist:300 was
# originally authored and shipped; it measured outcome=HIT S=25/A=8/R=9 — a
# declared-WHIFF that HITs — and has been REMOVED. HK's own item-18(b)
# R-clause and UNCLASSIFIED A-clause evidence (below, on BLOCK/HIT) is
# therefore CONTACT-LEGS-ONLY: a clean whiff was never observed for this
# strength, unlike LK/MK.
#
# SECTION ACCOUNTING (Phase 7): 8/8 entries carry a divergence (100%
# xfail — every leg has at least the item-18(b) R-clause, and BLOCK/HIT
# additionally carry the UNCLASSIFIED A-clause) — crosses the >50%-of-
# section threshold. (One entry, sean-ryuubi-hk-whiff, was REMOVED per the
# 2026-07-09 review fix — see HK WHIFF UNREACHABLE above — dropping the
# section from 9/9 to 8/8; still 100% xfail.) GRANTED by orchestrator at commit 2026-07-09 (review-verified post-fix: UOH/Roll routine buckets; Ryuubi item-18(b) LK/MK-whiff-verified + hedged UNCLASSIFIED A-overcount; Dragon Smash Strong oracle-duplicate question on the user-review list w/ the interpolation finding). User review pending (overnight autonomous run) (orchestrator
# grants at commit; per CORPUS-AUTHORING.md Phase 7, high-xfail sections
# require explicit sign-off, not self-approval — same posture as the
# Hugo-grab 55% precedent).

- label: sean-ryuubi-lk-whiff
  setup: { dist: 300, dummy: none }
  input: "motion qcf LK"
  qjson: { name: "Ryuubi Kyaku (Short)" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

- label: sean-ryuubi-mk-whiff
  setup: { dist: 300, dummy: none }
  input: "motion qcf MK"
  qjson: { name: "Ryuubi Kyaku (Forward)" }
  outcome: WHIFF
  expect: { S: from-qjson, A: from-qjson, R: from-qjson }

# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=10 is
# the engine-truth value on the BLOCK path. SWEEP-2 (2026-07-10) FULL
# CONVERT, xfail REMOVED. F3 (§13.13:5179-5184), own-ledger bar met: own
# ledger from s3-rundir/run-sean/run.log shows an inserted cell 68(add=2) on
# all six Ryuubi lk/mk-block/-hit legs, +2 exact vs arcade 8 (classification
# sweep #2 fable-grade audit). R remains arcade-exact (resolved 2026-07-09 by
# §13.5.1b).
- label: sean-ryuubi-lk-block
  setup: { dist: close, dummy: stand }
  input: "motion qcf LK"
  qjson: { name: "Ryuubi Kyaku (Short)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: 10, R: from-qjson, adv: from-qjson }

# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=10 is
# the engine-truth value on the HIT path - same F3 inserted-cell-68(add=2)
# ledger as sean-ryuubi-lk-block. SWEEP-2 (2026-07-10) FULL CONVERT, xfail
# REMOVED. R remains arcade-exact.
- label: sean-ryuubi-lk-hit
  setup: { dist: close, dummy: none }
  input: "motion qcf LK"
  qjson: { name: "Ryuubi Kyaku (Short)" }
  outcome: HIT
  expect: { S: from-qjson, A: 10, R: from-qjson }

# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=10 is
# the engine-truth value - same F3 inserted-cell-68(add=2) ledger as
# sean-ryuubi-lk-block. SWEEP-2 (2026-07-10) FULL CONVERT, xfail REMOVED. R
# remains arcade-exact.
- label: sean-ryuubi-mk-block
  setup: { dist: close, dummy: stand }
  input: "motion qcf MK"
  qjson: { name: "Ryuubi Kyaku (Forward)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: 10, R: from-qjson, adv: from-qjson }

# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=10 is
# the engine-truth value - same F3 ledger. SWEEP-2 (2026-07-10) FULL
# CONVERT, xfail REMOVED. R remains arcade-exact.
- label: sean-ryuubi-mk-hit
  setup: { dist: close, dummy: none }
  input: "motion qcf MK"
  qjson: { name: "Ryuubi Kyaku (Forward)" }
  outcome: HIT
  expect: { S: from-qjson, A: 10, R: from-qjson }

# HK contact legs at dist:250 (dist:close overshoots — see the section HK
# CONTACT DISTANCE note).
# §13.13 contact-path convention (adopted 2026-07-10, classification sweep #2
# fable-grade audit, disposition (a)-PARTIAL): A=10 is the engine-truth
# credit actually accrued on the BLOCK path - same F3 inserted-cell-68
# (add=2) own-ledger mechanism as sean-ryuubi-lk-block (own ledger,
# s3-rundir/run-sean/run.log). Arcade (whiff-canonical, from-qjson) A=8 -
# preserved here per the convention. Remaining fields: R/adv stay xfail on
# the stacked item 18(b)/§13.5.1b residual - the canonical full-corpus run
# measures R=14/adv=-2, still +1/-1 off arcade 15/-3 (mechanism open;
# bundle-context-dependent per §12.1 row 9's isolated-run finding, which
# does NOT undermine the golden/shipped R=14 value); S remains
# arcade-exact.
- label: sean-ryuubi-hk-block
  setup: { dist: 250, dummy: stand }
  input: "motion qcf HK"
  qjson: { name: "Ryuubi Kyaku (RH)" }
  outcome: BLOCK
  expect: { S: from-qjson, A: 10, R: from-qjson, adv: from-qjson, xfail: "DOCUMENTED HARNESS-BUNDLE-CONTEXT ARTIFACT (RE-DISPOSITIONED 2026-07-14, <sp>/zero/harness-bleed/footprint.md, opus-substitute / WANTS FABLE RE-REVIEW). The isolated single-entry run measures R=15/adv=-3 (arcade-exact, s3-rundir/run-sean/trace.log FINAL); the bundled full-suite run reads the bled R=14/adv=-2 asserted here, because the dummy's idle-breathing cell phase (charset.c:2982 hurtbox pulse) drifts with run position - which entry precedes this one in the corpus tape shifts the dummy's phase at contact (this leg's sibling legs lk/mk-block already read the isolated value in-tape; only the HK leg straddles the context-sensitive frame boundary). A global dummy-idle-reset (snapshot+restore plw[1].wu.{cg_ix,cg_ctr,cg_next_ix} at every corpus-entry label) was reconstructed and full-suite-tested this session: REJECTED as net-negative - it fixes only this row (R 14->15, adv -2->-3) but regresses 4 other rows off arcade (urien-msphere-hp-block, urien-crroundhouse-block, alex-crroundhouse-hit, yun-nishou-forward-hit - three of them make legitimately-connecting moves wrongly WHIFF). Stays XFAIL asserting the arcade value (R/adv via from-qjson); the corpus-frozen literal R=14/adv=-2 remains the bundled-context engine-truth value, unaffected by this finding. Golden unchanged. Prior audit trail (RELOCATED not deleted): NARROWED 2026-07-10 (§13.13, classification sweep #2 fable-grade audit, disposition (a)-PARTIAL): A now asserts the engine-truth value citing the same F3 inserted-cell-68(add=2) own-ledger mechanism as sean-ryuubi-lk-block (own ledger, s3-rundir/run-sean/run.log). PARTIALLY RESOLVED 2026-07-09 (§13.5.1b, see file header CORRECTED note): the item 18(b) R-truncation clause is MOSTLY gone (R was 8, is now 14, +6 from the gate refusing the false cut) but this leg's own pre-fix R was already 1 tick shorter than the other five contact legs' R=9 - post-fix it lands on R=14, still 1 tick short of arcade 15. dist:250 per the section HK CONTACT DISTANCE note." }

# §13.13 contact-path convention (adopted 2026-07-10, user decision): A=10 is
# the engine-truth value - same F3 inserted-cell-68(add=2) own-ledger
# mechanism as sean-ryuubi-lk-block. SWEEP-2 (2026-07-10) FULL CONVERT,
# xfail REMOVED - R resolves fully here (unlike sean-ryuubi-hk-block, this
# leg's pre-fix R was 9 like the other four contact legs, not the
# 1-tick-shorter 8), so no residual clause remains. R arcade-exact.
- label: sean-ryuubi-hk-hit
  setup: { dist: 250, dummy: none }
  input: "motion qcf HK"
  qjson: { name: "Ryuubi Kyaku (RH)" }
  outcome: HIT
  expect: { S: from-qjson, A: 10, R: from-qjson }

# --- Zenten / Sean Roll (motion qcb + P), all 3 strengths -------------------
# See file header ZENTEN / SEAN ROLL — CONFIRMED movement-only, Ashura
# posture. outcome=WHIFF only is assertable (a real FINAL fires with ZERO
# active frames — there is no BLOCK/HIT to script for a non-damaging dodge).
# SECTION ACCOUNTING (Phase 7): was 3/3 entries carry a divergence (100%
# xfail — the R field's own measurement-semantics gap) — crosses the
# >50%-of-section threshold, but this is the SAME already-reviewed Ashura
# Senkuu precedent bucket (corpus-akuma.yaml, review-ruled 2026-07-08), not
# a fresh sign-off request. GRANTED by orchestrator at commit 2026-07-09 (review-verified post-fix: UOH/Roll routine buckets; Ryuubi item-18(b) LK/MK-whiff-verified + hedged UNCLASSIFIED A-overcount; Dragon Smash Strong oracle-duplicate question on the user-review list w/ the interpolation finding). User review pending (overnight autonomous run) (orchestrator grants at
# commit, routine per the Ashura precedent).
# MASK-1 UPDATE (2026-07-11): the R field now field-masks
# (docs/frame-data-synthesis.md §13.15; new ERRATA §5, STRUCTURAL) on
# all three legs rather than staying asserted-and-xfailed. Section is
# now 0/3 xfail, 3/3 PASS-with-masked-R.

# FIELD-MASKED (ERRATA): R not asserted - zero-active movement-only
# dodge, whole duration classifies as saturated startup (measured
# S=T=28, A=0, R=0; T=28 exactly matches sean.json's own
# Startup+Hit+Recovery sum 2+19+7). Overlay R counts post-active ticks
# and there is no active anchor for a zero-active move — same
# measurement-semantics gap as corpus-akuma.yaml's akuma-ashura-whiff.
# Verdict STRUCTURAL, docs/arcade-frame-data/ERRATA.md §5 (new
# 2026-07-11; the cached FAT-3S snapshot's "LP Sean Roll" row
# independently matches sean.json's Recovery=7 exactly, corroborating
# the published figure — the class-based zero-active argument does not
# depend on this corroboration but it is recorded there). Oracle S=2/
# Hit=19/R=7 (all not independently comparable - no active anchor
# exists to decompose against, so S/Hit were never asserted either;
# recorded here for completeness). Comparable & asserted: outcome=WHIFF
# + finals=1 only (`expect: {}`). Masked-field drift stays golden-pinned
# (golden.py measured columns). MASK-1, user decision 2026-07-11,
# docs/frame-data-synthesis.md §13.15.
- label: sean-roll-lp-whiff
  setup: { dist: 250, dummy: none }
  input: "motion qcb LP"
  qjson: { name: "Sean Roll (Jab)" }
  outcome: WHIFF
  expect: {}

# FIELD-MASKED (ERRATA): R not asserted - same zero-active
# measurement-semantics gap as sean-roll-lp-whiff (see file header /
# ERRATA.md §5): measured S=T=38, A=0, R=0; T=38 exactly matches
# sean.json's own Startup+Hit+Recovery sum 2+29+7. Comparable & asserted:
# outcome=WHIFF + finals=1 only (`expect: {}`). MASK-1, user decision
# 2026-07-11, docs/frame-data-synthesis.md §13.15.
- label: sean-roll-mp-whiff
  setup: { dist: 250, dummy: none }
  input: "motion qcb MP"
  qjson: { name: "Sean Roll (Strong)" }
  outcome: WHIFF
  expect: {}

# FIELD-MASKED (ERRATA): R not asserted - same zero-active
# measurement-semantics gap as sean-roll-lp-whiff (see file header /
# ERRATA.md §5): measured S=T=52, A=0, R=0; T=52 exactly matches
# sean.json's own Startup+Hit+Recovery sum 2+43+7. Comparable & asserted:
# outcome=WHIFF + finals=1 only (`expect: {}`). MASK-1, user decision
# 2026-07-11, docs/frame-data-synthesis.md §13.15.
- label: sean-roll-hp-whiff
  setup: { dist: 250, dummy: none }
  input: "motion qcb HP"
  qjson: { name: "Sean Roll (Fierce)" }
  outcome: WHIFF
  expect: {}

# --- Negative control (plan section 1.8 item 5 precedent) ------------------

- label: sean-jump-none
  setup: { dist: 250, dummy: none }
  input: "press UP"
  outcome: NONE
