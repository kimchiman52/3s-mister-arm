# Rollback-determinism harness allowlist.
#
# Symbols listed here are EXPECTED to differ between a baseline run and a
# rollback run, for a verified benign reason stated on the entry. The
# driver never silently ignores anything: allowlisted hits are still
# printed (verdict ALLOWED, with the reason) — they just don't fail the
# run. Format: fnmatch pattern, optional `# reason` (reason mandatory by
# convention; review rejects reasonless entries).
#
# Rule of thumb: an entry belongs here only if the divergence is (a) an
# artifact of the harness/diagnostics themselves, or (b) state that is
# demonstrably write-only fan-out from the simulation (sound/render
# request sinks) whose divergence cannot feed back into gameplay. When in
# doubt, leave it OUT and let the run fail — a catalogued finding beats a
# hidden one. See docs/rollback-determinism-harness.md ("Reading the
# output").
#
# "Demonstrably" means a READ TRACE, not a category. Naming a symbol's
# subsystem is not evidence that it is write-only: that argument has now been
# caught wrong five times in this tree — spmv_ng_save (exonerated as dead code
# on the wrong character enum), ldreq_result and cseSysWork and mts_ok (each
# filed as an output sink while gating saved state), and ColorRAM (a palette
# buffer that really is a palette buffer, and still fed saved frw[] bytes).
# So an entry must say which reads exist and where each one lands. Reasons on
# the §A-derived blocks below were re-derived against the code on 2026-08-30;
# three of the sixteen failed. Task #138 then DE-ALLOWLISTED all three and
# ran the harness on each; the per-entry MEASURED blocks below carry the
# before/after DIVERGENT sets. If you add an entry, trace its reads first.

# --- rollback-path bookkeeping in src/netplay/game_state.c ---------------
# These are written by save_current_state()/save_state() — i.e. only when
# rollbacks happen — purely for the desync post-mortem dump. Never read by
# the simulation.
state_buffer               # desync-diag State ring (note_state), diag-only
saved_section_checksums    # per-section checksum ring for dump_desync_state, diag-only
saved_plw_scratch          # sanitized-PLW ring for dump_desync_state, diag-only
saved_field_hashes         # per-field hash ring for dump_desync_state, diag-only
save_current_state.*       # function-static sanitized-PLW scratch inside the save path
s_save_scratch             # save_state()'s scratch State (encoder input), diag/encode only
battle_start_frame         # first-save latch inside save_current_state, log-only

# --- harness's own process state ------------------------------------------
configuration              # holds the CLI flag values + const char* pointers into the argv block; the rollback run's flags differ from the baseline's BY CONSTRUCTION (--rbd-rollback-period)

# --- diagnostic counters / rate-limited log latches -----------------------
# Write-only counters read back only by debug prints / the desync dump.
g_random_16_calls          # pls02.c:622 RNG call counter, read only by dump_desync_state's fprintf (game_state.c:2324)
g_random_32_calls          # pls02.c:621, same (game_state.c:2323)
*.s_pctn_log               # ppgCheckTextureNumber rate-limited debug print counter
*.s_log_count              # bgDrawOneChip rate-limited debug print counter
*.s_dbg_count              # BG_Draw_System rate-limited debug print counter
flLogOut.bflLogOutFirst    # AcrSDK/ps2/foundaps2.c flLogOut's one-shot "write the acrout.txt banner" latch. Exactly one read, flLogOut's own `if (bflLogOutFirst != 0)`, whose arm does a flFileWrite of a fixed string and clears the latch; not extern'd, address never taken, no simulation reader. ADDED 2026-08-30 (task #138) after it surfaced DIVERGENT on the stock allowlist: it flips only in the rollback run because the ENABLE_PERF_TELEMETRY flLogOut inside Push_LDREQ_Queue's duplicate-drop guard (io/gd3rd.c) fires only on a resim leg. Read it as a MARKER of the rollback duplicate-push that guard exists to swallow -- the B run's log carries twelve `[ldreq-dedupe] Push_LDREQ_Queue dropped duplicate` lines and neither baseline carries any -- not as an independent escapee
fd_engine_*                # frame-data overlay engine probes (docs/plan-frame-data-harness.md); read only by the DEBUG training overlay/trace, reset per frame in game_step_0
fd_prev_active_cgix*       # frame-data overlay cg_ix edge-latch diagnostics, overlay-only consumers (frame_data_overlay.c)

# --- deliberately NOT allowlisted: charsel_active_effect_count ------------
# History: task #62 made pls03.c's 28 super-art command counters actually
# compile (the TU was missing the generated port/build_config.h, so every
# `#if ENABLE_PERF_TELEMETRY` in it silently evaluated to 0 -- no -Wundef).
# Once live they advanced on the harness's speculative-resim frames and
# needed the exact-symbol entry `perf_super_art_command_telemetry` here.
#
# That entry is GONE as of task #68. The counters, the accessor, the
# PLS03_SuperArtCommandTelemetry type and PLS03_Get/ResetSuperArtCommandTelemetry
# were deleted outright: the getter had zero callers, and its designed sink --
# the `test_state` object of the perf-capture JSON that
# tools/mister/perf-sampler.sh still greps -- no longer exists, having been
# removed with the legacy renderer perf surface ("Phase D rip",
# perf_capture_write_summary in port/sdl/sdl_app.c, which now emits only
# schema_version/frames/avg_frame_ms/fps). Nothing under src/ names any of
# those symbols any more.
#
# The one sibling task #62 revived that SURVIVES is charsel_active_effect_count
# (effect.c:21), and it is deliberately not listed. It is EXERCISED AND CLEAN:
# read straight out of this harness's own capture streams (fast mode, 1500
# frames x 2 scenarios, task #69) the baseline carries 30 distinct hashes for
# it in ryu-ken-basic-exchange and 24 in makoto-sa3-super -- it really does
# move -- and it matches the rollback run on every one of the 1500 frames in
# both. That is structural, not luck: it is assigned outright rather than
# accumulated -- charsel_active_effect_count = EFFECT_MAX - frwctr
# (mtrans.c) -- on every rendered frame, and frwctr IS in the save set
# (game_state.c:1804/1871/1944/2411). It is a pure function of restored state
# with no independent divergence surface; if frwctr drifted, the gate would
# flag frwctr. Its consumer is the DEBUG FPS overlay's sticky peak
# (fps_overlay_peak_active_effects), which task #68 wired into the overlay
# label's trailing `e<peak>` field -- a presenter-only sink that reaches
# neither gameplay, the save set, nor the checksum.
#
# The other two task #62 siblings, charsel_frame_portrait_tiles and
# charsel_frame_plate_tiles (formerly mtrans.c), were also deleted by task
# #68: they had zero readers, and they keyed on `mt->id == 13 / == 14` while
# texcash.c:660 assigns `mts[ix].id = ix` -- a cache-slot index, not a
# semantic identity, so the branch never meant "portrait"/"plate" at all.
# They also never left hash 0x776ec7e3 (= hash(int32 0)) across all 1500
# frames of both fast scenarios, i.e. the gate never drove them.
#
# If charsel_active_effect_count ever turns up DIVERGENT, that is a NEW
# finding and must be triaged, not silently added here.

# --- sound output sinks (research-desync-deep-investigation.md sect A.2.3) --
# Simulation writes requests; the SPU/CRI/ADX pipeline consumes them.
# Speculative rollback frames re-issue sound requests exactly as real
# netplay resim legs do (the known "rollback SE duplication" phenomenon).
#
# RE-DERIVED 2026-08-30. This block's original sentence was "Nothing in this
# class is read back by the simulation" — a CATEGORY judgement, admitted with
# no read trace, exactly the reasoning shape that had already failed on
# spmv_ng_save and on ldreq_result below. It has now failed a third time here:
# see the CORRECTION on cseSysWork. Note also what the §A.2.3 citation is
# actually worth. That section names bgm_exe/bgm_req/…/adx_stm_work and NOT
# ONE of the six symbols below, and its sweep universe was
# src/sf33rd/Source/Game/ only — `ram` lives in port/sound/, and cseSysWork /
# PhdAddr / gpTsb live in AcrSDK/MiddleWare/PS2/CapSndEng/, so they were never
# inside the document's scope at all. The traces recorded per entry were done
# against the code, not the document. Do not re-derive these by category;
# if you touch one, trace its READS.
ram                        # port/sound/spu.c `static u16 ram[]`, 2 MB emulated SPU wave RAM. Sole writer SPU_Upload (memcpy), sole caller cseSendBd2SpuWithId. Reads are confined to spu.c (SPU_VoiceDecode, SPU_VoiceStart); the ram-derived values reach `endx` (written, read nowhere in src/), and `envx`/`adsr_phase` -> SPU_VoiceIsFinished -> gcVoices -> allocVoice -> emlShimStartSound, which returns void and whose out-of-voices arm is a printf. SPU_VoiceGetConf has zero callers. Nothing crosses back into src/sf33rd/Source/Game/. VERIFIED write-only 2026-08-30
sdeb                       # sound/se.c SoundPatch dedup ring. Six reads: the self-shift inside Store_Sound_Code, plus five flPrintL arguments in Disp_Sound_Code — which returns early on Debug_w[4]==0 and whose flPrintL output terminates in the flDebugStrHan glyph buffer (port/sdl/sdl_debug_text.c). flPrintL returns a constant, discarded at all five sites. Not extern'd in any header, so no other TU can name it. VERIFIED write-only 2026-08-30
sdbd                       # sound/sound3rd.c `s8* sdbd[3]` sound data bank pointers. ZERO readers repo-wide — the only occurrences are the definition, the sound3rd.h extern, and two writes in rendering/color3rd.c (q_ldreq_color_data case 5, init_trans_color_ram case 10). Its address is never taken. VERIFIED write-only 2026-08-30, but it is written ONLY on the cseSysWork-gated LDREQ path below, so read it as a symptom marker of that divergence, not as an independent sink
#
# CORRECTION 2026-08-30 — cseSysWork IS READ BACK BY THE SIMULATION.
# The block sentence above is false for this member, and the entry's original
# reason ("CRI middleware sound work area") was a category label with no trace.
# The chain, every hop verified in the tree on 2026-08-30:
#
#   cseSysWork.SpuBankId[] (cse.c, file-static, NOT in GS_SAVE and never
#   rewound by a rollback)
#     -> read by cseGetIdStoredBd (cse.c)
#     -> the branch at the head of q_ldreq_color_data's `cfn->type == 10` arm
#        (rendering/color3rd.c): `if (cfn->data + 1 == cseGetIdStoredBd(...))`
#     -> taken: `*curr->result |= lpr_wrdata[curr->id]; curr->be = 0;` retires
#        the request IN THAT FRAME, skipping fsOpen, Pull_ramcnt_key, the async
#        AFS read, cseSendBd2SpuWithId, cseMemMapSetPhdAddr, cseTsbSetBankAddr
#        and the sdbd write. Not taken: the same result bit is set many frames
#        later at the end of case 5.
#     -> curr->result is &ldreq_result[i] (Push_LDREQ_Queue_* in io/gd3rd.c)
#     -> ldreq_result is read by Check_LDREQ_Queue_Player/_Union (io/gd3rd.c)
#     -> Check_PL_Load (system/sys_sub.c) -> Exit_6th (screen/sel_pl.c), whose
#        early return gates `Exit_Timer -= 1` and `Exit_No++`; and
#        Check_LDREQ_Queue_BG -> Bonus_Sub's stage wait (game.c) -> G_No/G_Timer
#     -> GS_SAVE(Exit_No), GS_SAVE(Exit_Timer), GS_SAVE(G_No), GS_SAVE(G_Timer)
#
# The write that makes it stateful is cseSendBd2SpuWithId (cse.c), which sets
# SpuBankId[bank] = id and is reached from color3rd.c on the simulation tick
# (Check_LDREQ_Queue, called from Game_Task in game.c). So a speculative leg
# that completes a sound-bank load permanently flips SpuBankId; the rollback
# restores GameState but not cseSysWork; and the confirmed leg then takes the
# OTHER arm. That is the textbook DIVERGENT+FEEDBACK shape.
#
# THIS ALSO EXPLAINS THE "wall-clock cluster". docs/rollback-determinism-
# harness.md records PhdAddr, cseSysWork, gpTsb, ldreq_result, ram, rckeyctr
# and sdbd drifting in and out of the diff together, and reads them as seven
# independent loader/sound sinks paced by disk timing. They are not
# independent: every one of the seven is written on the single
# q_ldreq_color_data type-10 path that this branch gates. One mechanism.
#
# MITIGATION, stated precisely rather than as a rebuttal. Both arms set the
# SAME result bit, so when Ldreq_BarrierActive() (io/gd3rd.c) is true the
# barrier drains the request inside the pumping frame either way and the
# frame-boundary ldreq_result is identical. Three limits on that: (a) the
# barrier is netplay-gated (TRANSITIONING/CONNECTING/RUNNING) and an rbd run
# has no session, so the harness's own cseSysWork divergence is unmitigated;
# (b) the drain bails on LDREQ_BARRIER_MAX_STEPS or the ms budget, logs, and
# re-exposes the phase difference; (c) the branch still decides whether
# Pull_ramcnt_key/Push_ramcnt_key run at all, which is where the rckey*
# divergence below actually comes from.
#
# THE ENTRY IS LEFT IN PLACE PENDING A DECISION, NOT BECAUSE IT IS BENIGN.
# Removing it turns the gate red, which is the point — it would let the
# harness see what it is currently hiding. If it stays it needs the plt_req
# treatment (a positive reachability + ordering proof), not a category
# sentence. Do not re-file it as a sound sink; it belongs with ldreq_result.
#
# MEASURED 2026-08-30 (task #138) — DE-ALLOWLISTED AND RUN, and THE PREDICTION
# ON RECORD IS REFUTED. The prediction was that cseSysWork would surface
# DIVERGENT *ahead of* ldreq_result/rckeyctr — the ordering technique that
# convicted ColorRAM. It surfaces BEHIND them, in both scenarios and at both
# cadences. tools/rollback-determinism/run.sh select on the Debug host build,
# both fast scenarios, this file with only the cseSysWork line removed:
#
#   select_period=8 (the shared gate): divergent 4 -> 6, feedback 0 -> 0. The
#   entire delta is cseSysWork itself, DIVERGENT (not DIVERGENT+FEEDBACK — it
#   is not a GS_SAVE symbol), TWO divergent frames per scenario, then it
#   reconverges and never differs again in 1500 frames.
#
# FIRST DIVERGENT FRAME PER SYMBOL, ryu-ken-basic-exchange / makoto-sa3-super,
# read off the same report — this is the substance of the refutation:
#
#   rckeyctr 208/208   plt_req 208/208   afs_handle 208/208   q_ldreq 208/208
#   ldreq_result 209/209   rckeymin 211/210   ColorRAM 212/213
#   ram 214/215   cseSysWork 214/215   PhdAddr 215/216   gpTsb 215/216
#   sdbd 215/216
#
# cseSysWork lands FIVE TO SIX FRAMES BEHIND ldreq_result and rckeyctr, in
# exactly the slot its own writer occupies in the pipeline: SpuBankId is set by
# cseSendBd2SpuWithId, and q_ldreq_color_data reaches that call in `case 4`,
# after the async read has completed — long after `case 2` took the ramcnt key
# and after *curr->result was first touched. In these runs cseSysWork is a
# DOWNSTREAM SYMPTOM of the one LDREQ phase shift, the status this block
# already assigns to sdbd — not the upstream cause of ldreq_result's
# divergence. The "one mechanism, not seven independent sinks" reading above
# survives; the direction of causation inside it does not.
#
# ADVERSARIAL CONTROL, because a 2-frame window is a weak negative.
# --select-rollback-period 1 (8x the select-phase rollback cadence; see known
# limit 1 in the harness doc for why period 1 is tracked separately), same
# de-allowlisted file: divergent 8, feedback 0. cseSysWork's window widens from
# 2 frames to NINETEEN (197..215 / 196..216) and the ordering is unchanged —
# rckeyctr/plt_req 193, ldreq_result 194, ram 196/197, cseSysWork 196/197,
# sdbd 197/198. So even with SpuBankId in disagreement for nineteen consecutive
# frames across the whole of character select, Exit_No, Exit_Timer, G_No and
# G_Timer stay byte-identical on all 1500 frames of both scenarios. (The other
# two DIVERGENT rows at period 1 are Candidate_Buff, which this file
# deliberately does not allowlist and which the block below already names, and
# flLogOut.bflLogOutFirst, now allowlisted at the top.)
#
# WHY IT SELF-HEALS. Both timelines complete the SAME bank load and write the
# same id, so cseSysWork reconverges on its own; and both arms of the branch
# set the same result bit, so ldreq_result reconverges with it. The window
# closes 111 frames before in-game (G_No[1]==2 at frame 328) and long before
# Exit_6th can be dispatched, which needs Exit_No == 5.
#
# THIS IS NOT AN EXONERATION, and the ColorRAM precedent is exactly why: a
# scenario-bounded feedback=0 means "no scenario reaches it yet". The branch at
# the head of q_ldreq_color_data's type-10 arm is still an UNSAVED gate on a
# path that writes SAVED state, and it is still the only member of this block
# with that shape. What bounds it is CADENCE, not structure — the type-10
# requests these scenarios issue are spaced further apart than the window in
# which SpuBankId disagrees, and widening the window 10x did not close that
# gap. REOPEN IF: a type-10 request is pumped while SpuBankId is in
# disagreement (observable as ldreq_result, Exit_No, Exit_Timer, G_No or
# G_Timer moving with cseSysWork de-allowlisted), or cseSysWork stops
# reconverging, or a scenario is added in which the two legs load DIFFERENT
# bank ids rather than the same one at different frames.
#
# RELATIONSHIP TO THE LDREQ ROLLBACK LEAK — they are TWO mechanisms on one
# path, and the difference is the direction. The leak is a SAVED gate (SP_No,
# incremented by Sel_PL_3rd in screen/sel_pl.c) with an UNSAVED side-effect
# ledger (q_ldreq, texgrplds, the rckey allocator), so rollback replays the
# side effect and it accumulates — a heap leak that ends in a crash, invisible
# to this harness by construction. cseSysWork is the mirror image: an UNSAVED
# gate (SpuBankId) in front of a branch whose consumer state IS saved, so
# rollback can make the two legs take different arms — a divergence, which is
# precisely what this harness is for. Same q_ldreq_color_data pump, opposite
# save-set asymmetry. Fixing one does not fix the other. The
# `[ldreq-dedupe] Push_LDREQ_Queue dropped duplicate` lines that appear only in
# the rollback run's log (see the flLogOut.bflLogOutFirst entry at the top) are
# the leak's mechanism being caught by its own guard, in the same runs.
cseSysWork                 # CRI middleware sound work area — REASON FALSIFIED, SEE THE CORRECTION ABOVE. Feeds ldreq_result and hence GS_SAVE(Exit_No)/(Exit_Timer)/(G_No)/(G_Timer). MEASURED 2026-08-30 (task #138): de-allowlisted it surfaces DIVERGENT but BEHIND ldreq_result, self-heals in 2 frames (19 at select period 1), and moves no saved consumer — kept on that CADENCE bound, not on the falsified category. Honour the reopen condition
PhdAddr                    # CRI PHD (sound header) pointer table, AcrSDK/…/CapSndEng/emlMemMap.c. Exactly one read, mlMemMapGetPhdAddr, whose one caller is PlaySe (emlSndDrv.c). pHEAD feeds GetNumSplit/GetPhdParam/CalcPhdParam (emlRefPhd.c declares no globals) then StartSound -> emlShimStartSound, whose entire mutable state is file-static in port/sound/emlShim.c. Every return in that stack is a hard-coded 0 and the sole game-side caller discards it. No file under src/sf33rd/Source/Game/ names it. VERIFIED write-only 2026-08-30
gpTsb                      # CRI TSB (sound bank) pointer table, AcrSDK/…/CapSndEng/emlTSB.c. Exactly one read, mlTsbGetDataAdrs; the resulting pTSB drives mlTsbRequest's cmd switch and is copied field-by-field into a STACK-LOCAL CSE_REQP. The switch arms reach only EchoWork (which has no reader outside emlTSB.c), the emlShim statics and SPU voice registers. Every return in the stack is 0. VERIFIED write-only 2026-08-30. Latent hazard, not a desync: mlTsbSetBankAddr lacks the bounds check its PhdAddr twin has; bank is currently <= 3

# --- render / texture / palette sinks (research doc sect A.2.2, A.2.4) ------
# Written by sim-side request pushes, consumed by the renderer/loader.
#
# RE-DERIVED 2026-08-30, same treatment as the sound block, and with the same
# result: the block sentence is not true of every member. mts_ok FAILED the
# trace (CORRECTION below) and ColorRAM's residual claim is now UNDETERMINED.
# What the §A citation is worth here: §A.2.2 does name ColorRAM, colPalBuffDC
# and mts_ok, but §A.2.4 names PrioBase/cmtx/bg_priority/TopHUD* and NOT ONE
# symbol on this list; and `palettes` + `latest_texture_spec`
# (src/platform/video/software) and texcash_melt_buffer_mem + tpu_free_mem
# (src/main.c) sit outside the §A.2 sweep universe (src/sf33rd/Source/Game/)
# altogether. Five of these eight were never in the cited document at all.
palettes                   # software-renderer palette store, platform/video/software/software_renderer.c, `static` so no other TU can name it. Sole writer Renderer_CreatePalette; three reads, all `pal = &palettes[...]` in the rasterizers, terminating in the static `canvas` -> SoftwareRenderer_GetCanvas -> port/sdl/sdl_app.c presentation. Checked specifically for a read-back-out: there is no "already uploaded" early-out, no refcount and no LRU; the one cross-call read-back is `version`, consumed by the colour-key scratch cache, whose output is pixels. VERIFIED write-only 2026-08-30
ColorRAM                   # rendering/color3rd palette transfer RAM. See the ROW ARITHMETIC and MEASURED blocks below: pinned rows are 0/8/16/24 entries [0..11] only, the metamor launder writes all four of them, and a Twelve-vs-Makoto run found it carrying nothing divergent. PARTIALLY EXEMPT: the four row-prefixes effl8 latches from (ColorRAM[0]/[8]/[16]/[24], first 12 u16) ARE in the save set as GameState.effl8_colorram — they fed back into saved frw[] bytes and caused the persistent makoto-sa3-super frw divergence. THE RESIDUAL CLAIM IS NO LONGER "no path back into simulation state" — SEE THE CORRECTION BELOW. If a NEW ColorRAM-mediated feedback shows up, widen the save set; do not widen this entry.
#
# CORRECTION 2026-08-30 — the residual ColorRAM claim is UNDETERMINED, not
# proven. This entry used to end "The rest of the array is render fan-out with
# no path back into simulation state." A second path exists, so that wording
# was too strong; what is undetermined is whether it is REALIZABLE, not
# whether it is there. Enumerated:
#
#   metamor_color_store (rendering/meta_col.c) READS ColorRAM rows 0..3 and
#   mcs_sel_tbl[wkid] into metamor_original[2][5][64], which is NOT in
#   GS_SAVE and is not rewound by a rollback. metamor_color_restore then
#   writes it back to ColorRAM[i + wkid*16] AND ColorRAM[i + wkid*16 + 8] —
#   for i == 0 that is rows 0 and 8 (wkid 0) / 16 and 24 (wkid 1), i.e.
#   EXACTLY the rows GameState.effl8_colorram covers, and note the second arm
#   is a CROSS-ROW launder (row 8 receives what was stored from row 0).
#   effl8's save_old_color_data then latches those bytes into the saved frw
#   window. metamor_color_restore runs in battle simulation (effk7.c,
#   plmain.c, plmain2.c).
#
# WHAT BLOCKS A VERDICT. metamor_original's only writer is
# metamor_color_store, whose only callers are in init_trans_color_ram's case 3
# (rendering/color3rd.c) — the async LDREQ completion path again, pumped by
# Check_LDREQ_Queue on the simulation tick. Whether a type-3 colour load can
# actually complete INSIDE a rollback window with Twelve in the match was not
# established by reading. Settle it with a harness run on a Twelve mirror with
# ColorRAM de-allowlisted, or by instrumenting metamor_color_store's frame
# against the rollback window. Until then this is an open question, not a
# cleared one. (The same "only path from ColorRAM into saved state" wording in
# src/netplay/game_state.c's GameState_Load comment was corrected alongside
# this note.)
#
# ROW ARITHMETIC 2026-08-30 (task #138) — the durable fact the correction above
# was missing. Stated once, here, so nobody re-derives it.
#
#   PINNED (saved by GameState_Save, restored by GameState_Load): ColorRAM rows
#   0, 8, 16, 24 — and only entries [0..11] of each. The rows come from
#   EFFL8_STEP_ROW / EFFL8_MOVE_ROW (effect/effl8.h) through
#   EFFL8_COLORRAM_ROW_LIST (src/netplay/game_state.c); the width is
#   GameState.effl8_colorram's second dimension, 12 of the 64 u16 in a
#   ColorRAM row, tied to EFFL8_COLOR_ENTRIES by a _Static_assert.
#
#   metamor_color_store READS rows wkid*16 + 0..3 plus row mcs_sel_tbl[wkid]
#   (0x1F8 / 0x1FC), all 64 entries, into metamor_original.
#   metamor_color_restore WRITES rows wkid*16 + 0..3, rows wkid*16 + 8..11 and
#   row mcs_sel_tbl[wkid], all 64 entries.
#
#   INTERSECTION: NON-EMPTY, and neither covered nor disjoint. restore's i == 0
#   arm writes rows wkid*16 and wkid*16 + 8 — rows 0 and 8 for wkid 0, rows 16
#   and 24 for wkid 1, i.e. all four pinned rows, entries [0..11] included. The
#   pin holds 12 of the 64 columns metamor writes.
#
#   BUT the only metamor cell that can reach saved state is
#   metamor_original[wkid][0][0..11]. save_old_color_data (effl8.c) reads
#   exactly EFFL8_COLOR_ENTRIES s16 from step_xy_table, and step_xy_table is
#   (s16*)ColorRAM[row], so the sole ColorRAM -> frw read in the tree lies
#   inside the pinned window. restore's i == 1..3 arms land in rows 1-3 / 9-11
#   / 17-19 / 25-27 and its mcs_sel_tbl arm in row 504 / 508; none of those is
#   pinned, and none has a reader that reaches saved state — effj7's
#   get_new_color_data WRITES rows master_id*16 and +8 from a ROM table rather
#   than reading them, and mtrans/color3rd read ColorRAM only into render
#   fan-out.
#
# THE PRESCRIBED SCENARIO WAS WRONG. "A Twelve mirror" cannot settle this.
# Every metamor_color_restore call passes the TWELVE player's own slot id
# (mwk->wu.id / wk->wu.id, under a player_number == CHAR_TWELVE gate in
# engine/plmain.c's player_mv_0000, engine/plmain2.c and effect/effk7.c), while
# effl8's master_id is the MAKOTO player's own slot (effect_L8_init returns 0
# unless wk->player_number == 16, then sets ewk->master_id = wk->wu.id). One
# slot cannot be both, so in a Twelve MIRROR effl8 never spawns and there is no
# ColorRAM -> frw read to poison at all. The overlap needs ONE slot to be
# Twelve when restore runs and Makoto when effl8 latches — i.e. Twelve after
# X.C.O.P.Y. has set wk->player_number to the opponent's character, which
# K7_muriyari_metamor_rebirth (effect/effk7.c) later undoes by reassigning
# My_char[wk->wu.id]. The scenario is TWELVE vs MAKOTO with Twelve copying, not
# a mirror.
#
# MEASURED 2026-08-30 (task #138) — DE-ALLOWLISTED AND RUN, three ways.
#
#   (a) Both stock fast scenarios, ColorRAM removed from this file:
#       divergent 4 -> 6, feedback 0. ColorRAM diverges on exactly two frames
#       per scenario — 212,213 (ryu-ken-basic-exchange) and 213,214
#       (makoto-sa3-super) — i.e. inside the select-phase LDREQ palette load,
#       nowhere else in 1500 frames. metamor_original and hi_meta hold ONE
#       distinct hash for the whole run in both: the metamor path is never
#       entered, so those runs said nothing about it.
#
#   (b) TWELVE vs MAKOTO, the scenario the paragraph above derives:
#       makoto-sa3-super with --test-p1-character 18 --test-p2-character 16
#       --test-p1-super-art 2, ColorRAM removed. The metamor path IS entered
#       for the first time in this harness — metamor_original and hi_meta each
#       take TWO distinct hashes instead of one, i.e. init_trans_color_ram's
#       case 3 ran metamor_color_store, and player_mv_0000's unconditional
#       `player_number == CHAR_TWELVE` restore ran the store->restore launder
#       into the pinned rows (Twelve is P1, so wkid 0, so rows 0 and 8).
#       Result: ColorRAM gains two NEW in-battle divergent frames, 1059 and
#       1060, on top of the select-phase pair at 211,212 — so the scenario is
#       not vacuous. metamor_original is BYTE-IDENTICAL between the baseline
#       and the rollback run on all 1500 frames, and so is frw. feedback 0.
#
#   (c) The adversarial control on the cseSysWork entry
#       (--select-rollback-period 1) leaves ColorRAM's behaviour unchanged.
#
# VERDICT: the launder exists, it is now EXERCISED, and it carries nothing
# divergent — metamor_original never differs, so restore can only write back
# what both timelines already agreed on. That is a much narrower residual than
# "undetermined", but it is still not a clearance, for one named reason: the
# X.C.O.P.Y. SUPER never activated in run (b).
# perf_metamorphose_active_frames (port/sdl/sdl_app.c) never leaves its initial
# value, so effect_K7's in-battle metamor_color_restore calls — the ones that
# run while a rollback window is open, as opposed to the one at round init —
# were not exercised. SETTLE IT by making Twelve actually land X.C.O.P.Y.
# inside the battle (a preset that scripts the SA and asserts
# perf_metamorphose_active_frames > 0), then re-run (b). Until then: store side
# exercised and clean, in-battle restore side still unexercised.
colPalBuffDC               # rendering/color3rd DC palette buffer. Three occurrences in the whole tree: the definition, the write in palCopyGhostDC, and one read in palUpdateGhostDC feeding palConvRowTim2CI8Clut into VRAM locked by flLockPalette. Checked for the read-back shapes: no "already converted" test, no diff/compare, no reuse check, no address taken, no sizeof, no alias escaping the file. VERIFIED write-only 2026-08-30
latest_texture_spec        # platform/video/software/software_renderer.c current-texture latch. REASON CORRECTED 2026-08-30: it is NOT a batching memo or cache. Its eight occurrences contain no ==, != or memcmp — nothing ever compares it, so no stale value can gate or skip work. Four unconditional writes in Renderer_SetTexture (void) plus the Init reset; three reads, each a plain struct copy into quads[]. The struct is two int16_t handle indices, not pointers, so process layout cannot move it. VERIFIED write-only 2026-08-30
#
# CORRECTION 2026-08-30 — mts_ok IS READ BACK BY THE SIMULATION.
# "consumed by the renderer/loader" is false for this member. Two chains, both
# verified in the tree on 2026-08-30, both landing in the rollback save set:
#
#   A. get_my_trans_mode (rendering/texcash.c) returns -1 when
#      mts_ok[curr].be == 0, else mts[curr].mode. Its return is stored into
#      ewk->wu.my_trans_mode at roughly 110 effect call sites. my_trans_mode
#      is in PLW_CANON_FIELD_LIST (src/netplay/plw_canon_fields.h) and lives
#      in the frw[] pool that GameState_Save/Load copy wholesale; it is NOT
#      zeroed by sanitize_work_rendering, so it is inside the canonical hash.
#
#   B. Mtrans_use_trans_mode (rendering/aboutspr.c) opens with
#      `if (mts_ok[wk->my_mts].be == 0) return;` — and that gate sits ABOVE
#      the No_Trans early return, defeating the invariant the comment two
#      lines below it was written to protect ("Above No_Trans so a frame that
#      skips drawing lands on the same value"). Taking it skips
#      `wk->my_clear_level = 0x90` and `wk->current_colcd &= 0x1FF`. Both are
#      PLW canon fields under GS_SAVE(plw); my_clear_level is not sanitized at
#      all, and current_colcd's sanitizer masks only ~0x2000 while the skipped
#      write clears everything above 0x1FF. Reached on PLW through
#      reqPlayerDraw -> sort_push_request(&plw[N].wu), inside njUserMain,
#      which step_game runs on rolled-back frames.
#
# REALIZABILITY, stated honestly. mts_ok[].be flips only on texture-cache slot
# creation and teardown (make_texcash_work / purge_texcash_work), not per
# frame — the per-frame churn is in min16/min32/mincg. make_texcash_work's
# call sites are simulation-side and NOT behind No_Trans (stage/bg init,
# game.c, effe6.c, ranking.c), so a speculative leg that crosses one leaves
# be == 1 behind and the confirmed leg then takes the other arm. What was NOT
# established by reading is whether a real rollback window straddles one of
# those calls in a live session; that needs a harness run with mts_ok
# de-allowlisted. The reason string is falsified either way.
#
# MEASURED 2026-08-30 (task #138) — DE-ALLOWLISTED AND RUN. NULL RESULT, and
# the null is stronger than a feedback=0. tools/rollback-determinism/run.sh
# select on the Debug host build, both fast scenarios, select_depth=8
# select_period=8, this file with only the mts_ok line removed:
# divergent 4 -> 4, allowlisted 66 -> 66, feedback 0. mts_ok does not appear in
# the A1-vs-B diff AT ALL in either scenario — the pattern never matched, i.e.
# the symbol is byte-identical between the baseline and the rollback run on
# every one of the 1500 frames. That is a different (and better) result than
# ColorRAM's "diverges, but nothing downstream moved".
#
# WHY, established by reading rather than left as "no scenario reaches it yet".
# mts_ok[].be flips only in make_texcash_work / purge_texcash_work
# (rendering/texcash.c) and in init_texcash_1st. The slots that matter are the
# two plw[] entries draw with: setup_base_and_other_data (engine/plcnt.c)
# assigns plw[0].wu.my_mts = 3 and plw[1].wu.my_mts = 4, and wu.my_mts is
# itself a PLW canon field (src/netplay/plw_canon_fields.h), so only `be` is
# unsaved. Every purge/make pair on those two slots is SAME-FRAME:
# Purge_texcash_of_list(3) is immediately followed by Make_texcash_of_list(3)
# at every one of its call sites — game.c's Select_Player/Switch_Screen arm,
# its Next_CPU arm, its After_Bonus arm and its Next_Q arm, plus menu/menu.c
# and demo/demo02.c — and pto_list[3] and mto_list[3] (rendering/mmtmcnt.c)
# both name slots 3 and 4. A frame boundary cannot land between them, so the
# per-frame hash never observes be == 0 for a live PLW slot.
#
# The one site that leaves a PLW slot purged ACROSS frames is the bonus-stage
# entry in engine/bbbscom2.c — purge_texcash_work(4) / purge_texcash_work(3),
# reassign wk->wu.my_mts, then make_texcash_work(5) — and it is UNREACHABLE IN
# MODE_NETWORK. Bonus_Type's only non-zero writer is Check_Bonus_Type() in
# screen/next_cpu.c, inside Next_CPU, i.e. Game05, which is entered solely from
# the `default:` arm of Game03's Mode_Type switch that
# `case MODE_VERSUS: case MODE_NETWORK:` diverts past (game.c) — the same gate
# the plt_req proof further down rests on.
#
# The other cross-frame window is character-select entry, and it is benign for
# a different reason: Purge_mmtm_area(2) (screen/sel_pl.c) purges slots 3 and 4
# through pto_list[2], and the Make_texcash_of_list(2) on the next line remakes
# only 12/13/14 (mto_list[2]), so be == 0 for slots 3 and 4 for the whole of
# select. Nothing reads it, because plw[N].wu.my_mts is not assigned until
# setup_base_and_other_data, which runs after game.c's Make_texcash_of_list(3)
# has put those slots back.
#
# CORRECTION TO THE CORRECTION. The block above says the be == 0 gate defeats
# the No_Trans invariant for BOTH `wk->my_clear_level = 0x90` and
# `wk->current_colcd &= 0x1FF`. Only the first is above the guard: read
# Mtrans_use_trans_mode (rendering/aboutspr.c) and `current_colcd &= 0x1FF`
# sits BELOW `if (No_Trans) return;`, so it is already skipped on every
# No_Trans frame by design. The be gate does add a skip on NON-No_Trans frames,
# so current_colcd is still at risk — but not for the reason the correction
# gave. my_clear_level is the field the quoted invariant was written for.
#
# STATUS: the reason string stays falsified — mts_ok is NOT "consumed by the
# renderer/loader" — but the entry is now admitted on REACHABILITY, like ppwork
# below, and no longer on a category. VOIDING CONDITION — delete this entry and
# re-open the finding if ANY of: a purge of slot 3 or 4 stops being followed by
# its make in the same frame; Bonus_Type becomes settable under MODE_NETWORK;
# plw[N].wu.my_mts starts pointing at a slot mto_list[3] does not remake; or
# Mtrans_use_trans_mode's `be` gate moves relative to the No_Trans return.
mts_ok                     # rendering sprite-dispatch ready flags (aboutspr/texcash) — REASON FALSIFIED, SEE THE CORRECTION ABOVE. Reaches GS_SAVE(plw) canon fields my_clear_level/current_colcd and the saved frw field my_trans_mode. MEASURED CLEAN 2026-08-30 (task #138): de-allowlisted, it never enters the diff at all — now admitted on NETPLAY UNREACHABILITY, not write-only-ness. Honour the voiding condition
texcash_melt_buffer_mem    # main.c static scratch handed to the texture cache. Never written through its own name; its single alias texcash_melt_buffer has one consumer, mts[ix].mltbuf, and every one of the 27 mltbuf uses is either the destination of lz_ext_p6_fx/_cx or the source of njReLoadTexturePartNumG -> ppgRenewDotDataSeqs (void, copies into texture source RAM). njReLoadTexturePartNumG returns a constant that all 14 call sites discard. No byte ever reaches a branch, index, count or loop bound. VERIFIED write-only 2026-08-30
tpu_free_mem               # main.c static scratch handed to the TPU allocator. This one DOES have the risky shape — get_mltbuf16_ext/32_ext return slot handles read out of tpu_free->x16_used/x32_used — so it was traced rather than assumed: every returned handle terminates in njReLoadTexturePartNumG (texture upload) or seqsStoreChip, which writes only seqs_w and whose sole zero-return condition is the sprite list being full, never a tpu_free value. Pool exhaustion is never branched on by simulation code. The render path writes no WORK field at all (`grep -E "wk->[A-Za-z_0-9.\[\]]+ *=[^=]"` over mtrans.c and texcash.c is empty). VERIFIED write-only 2026-08-30
sa_frame                   # ui/sc_sub.c SA-gauge HUD frame display state (cockpit rendering). Exactly four reads, all inside Sa_frame_Write, all supplying scfont_put arguments; scfont_put writes only scrscrntex[] and returns void. Sa_frame_Write returns early on omop_cockpit == 0 AND on No_Trans, so it does not execute on a rollback resim frame at all. No address taken, no sizeof, no memcpy, not inside a struct copied wholesale. VERIFIED write-only 2026-08-30

# --- loader / RAM-allocator state perturbed by speculative asset requests --
# The speculative legs re-issue load/alloc requests the restore cannot
# rewind (queue state is deliberately outside GameState). Real netplay
# rollbacks perturb these identically.
#
# CORRECTION 2026-08-24 (task #60). This block used to end with "they are
# consumed by the loader, not read by the simulation". That is FALSE for
# ldreq_result and it is the reason this block hides a real desync class:
#
#   ldreq_result is read by Check_LDREQ_Queue_Player / _Union / _Direct
#   (io/gd3rd.c), which gate Exit_6th (screen/sel_pl.c:1701-1722 -> Exit_No,
#   Exit_Timer) and Bonus_Sub's stage wait (game.c:1377-1381 -> G_No, G_Timer).
#   Exit_No/Exit_Timer/G_No/G_Timer are ALL in the rollback save set
#   (netplay/game_state.c:456/607/453/540). Saved state is therefore derived
#   from an allowlisted, unsaved symbol -- the textbook DIVERGENT+FEEDBACK
#   shape, which this allowlist entry suppresses by construction.
#
# These entries stay, because the divergence is not fixable by save-set
# membership: afs_handle is an index into port/io/afs.c's requests[] table
# whose SDL_AsyncIO* and read cursor cannot be rewound, and AFS_Read is a
# real async disk read (afs.c:366), so completion timing is wall-clock, not
# frame-count. The empirical proof is in this harness's own output: q_ldreq,
# rckey_work, rckey_mmobj, texgrplds, char_init_data, requests and afs all
# appear in the A1-vs-A2 BASELINE NOISE list -- they differ between two
# identical no-rollback runs of the same binary on the same machine. Two peers
# cannot agree on them either, and no local save/replay scheme changes that.
# The correct treatment is a load barrier (keep the simulation from observing
# an in-flight LDREQ load while a session is running), not a wider save set.
#
# CORRECTION 2026-08-25 (task #65): asyncio_queue used to be named in that
# list and no longer belongs in it. It is a whole-pointer static
# (SDL_AsyncIOQueue*, port/io/afs.c:47), so it is now pointer-canonicalized in
# the capture and holds one identical hash in every run. Its former noise
# membership was evidence of ALLOCATOR nondeterminism -- it only ever carried
# the queue's address, never its contents -- not of loader timing. The genuinely
# wall-clock members above are unaffected and still noise-masked, so nothing
# about this block's conclusion changes.
#
# UPDATE 2026-08-24 (task #66): that barrier now exists. Check_LDREQ_Queue()
# (io/gd3rd.c) finishes every queued request inside the simulated frame that
# pumped it whenever Ldreq_BarrierActive() -- i.e. whenever the GekkoNet
# session is RUNNING. The queue is therefore empty and afs_handle is AFS_NONE
# at every simulated-frame boundary IN NETPLAY, which makes ldreq_result a
# pure function of the Push_LDREQ_Queue_* sequence (driven by SP_No/G_No/
# bg_w.stage, all saved).
#
# These entries still stay, and this harness's verdict is unchanged, because
# the barrier is deliberately netplay-gated: an rbd run has no session and no
# --ldreq-barrier-force, so it takes the stock unbarriered path and these
# symbols keep behaving exactly as described above. The barrier is validated
# by tools/ldreq-timing (which varies loader wall-clock rather than rollback
# cadence) -- see known limit 9 in docs/rollback-determinism-harness.md for
# why this harness cannot do that job.
#
# Do NOT quietly widen this block. Candidate_Buff still diverges at
# --rbd-select-rollback-period 1 and is deliberately left unallowlisted.
#
# UPDATE 2026-08-25 (task #69): plt_req IS now allowlisted, below, and it is
# the only member of this block admitted on a positive reachability proof
# rather than on "the loader owns it". The refusal it replaces was correct
# for its time -- nobody had measured it. What was measured:
#
#   VALUE. plt_req[id] is written only by Push_LDREQ_Queue_Player
#   (io/gd3rd.c:384), which sets it to the character whose requests it
#   enqueues in the same call. Every call site reachable in MODE_NETWORK
#   passes My_char[id] (sel_pl.c:790, :996, win.c:178, menu.c:1571-1572,
#   ranking.c:330-331) and My_char is GS_SAVE'd (game_state.c:232/972). The
#   only site that passes anything else -- Push_LDREQ_Queue_Player(COM_id,
#   17), next_cpu.c:958 -- sits under Game05, entered solely from the
#   `default:` arm of Game03's Mode_Type switch (game.c:817), which
#   `case MODE_VERSUS: case MODE_NETWORK:` divert past at game.c:786-787.
#   So the divergence is a PHASE LEAD of a value both timelines agree on:
#
#     plt_req1  no-rollback  0 -> 11 at frame 210
#     plt_req1  rollback(8)  0 -> 11 at frame 208
#
#   ORDER. The only reader reachable during character select is Exit_6th
#   (sel_pl.c:1701-1706) via Check_PL_Load (sys_sub.c:899) ->
#   Check_LDREQ_Queue_Player (gd3rd.c:780-786). Exit_6th is dispatched only
#   at Exit_No == 5 (sel_pl.c:1551-1553), and Exit_No leaves 0 only through
#   Exit_1st (sel_pl.c:1556-1562), which returns early unless BOTH
#   operator-active players have Sel_Arts_Complete < 0 (set at sel_pl.c:865,
#   strictly downstream of the PL_Sel_1st push at sel_pl.c:788-790, itself
#   downstream of the Sel_PL_3rd confirm push at sel_pl.c:984-996).
#   Sel_PL_Complete / Sel_Arts_Complete / Exit_No / SP_No are all saved
#   (game_state.c:253/993, 550/1290, 456/1196, 457/1197), so both peers
#   agree on that ordering exactly. The confirmed-timeline write therefore
#   always lands before the reader can be dispatched.
#
#   EVIDENCE. tools/ldreq-timing's per-frame trace driven by --rbd-*
#   rollback injection instead of injected AFS latency, 1500 frames, both
#   fast scenarios x barrier off/forced-on x select period 8 and the
#   adversarial period 1, select depth 8, ASLR off, pinned RNG. plt_req
#   diverges on 2 frames (period 8) / 7 frames (period 1) per slot; every
#   SAVED column (G_No[0..3], G_Timer, Exit_No, Exit_Timer) is
#   byte-identical on all 1500 frames in all TWELVE rollback-vs-baseline
#   comparisons (2 scenarios x barrier off/on x select {d8p8, d2p8, d8p1});
#   and
#   Exit_No first reaches 5 at frame 317 in all SIXTEEN traces -- 108 frames
#   after the window closes.
#
#   VOIDING CONDITION. Add a reader of plt_req, Check_PL_Load or
#   Check_LDREQ_Clear inside character select before Exit_No leaves 0 and
#   this entry is wrong. Delete it and re-open the finding; do not widen it.
afs_handle                 # AFS async file-system handle bookkeeping
plt_req                    # io/gd3rd.c:162 pending-load character latch — phase lead of a saved value (My_char), read only by Exit_6th which cannot be dispatched until after the confirmed-timeline write. SEE THE PROOF ABOVE
ldreq_result               # io/gd3rd.c load-request result slots — SEE CORRECTION ABOVE
rckeyctr                   # system/ramcnt.c RAM-key allocator counter
rckeymin                   # system/ramcnt.c RAM-key allocator low-water mark
rckeyque                   # system/ramcnt.c RAM-key allocator queue

# --- rumble sink (research doc sect A.2.2) ----------------------------------
#
# RE-DERIVED 2026-08-30. Both are genuinely named in §A.2.2, unlike most of
# the two blocks above. vib_req cleared; ppwork's reason is FALSE, though the
# path it opens is currently blocked — read the voiding condition, because
# this entry is only correct for as long as three unrelated gates hold.
#
# Timing note that applies to both: move_pulpul_work is called from Game_Task
# (game.c) INSIDE the `for (ix = 0; ix < ff; ix++)` simulation loop and
# OUTSIDE the `if (!No_Trans)` guard that was put around texture_cash_update.
# So the rumble queue drains once per replayed logic frame, rollback resim
# frames included. That asymmetry is why these diverge; it is not itself a bug
# because the only observable output is sceVibSetActParam -> SDLPad_RumblePad
# (port/sdk/sdk_libpad2.c), which unconditionally returns 1, so the state
# machine's branch on it is constant.
#
# CORRECTION 2026-08-30 — ppwork is not write-only; it is currently UNREACHED.
# The reason "rumble device work" implied write-only fan-out. In fact
# ppwork[id].ok_dev is read in Button_Config_Sub (menu/menu.c) and gates
# `Convert_Buff[1][id][8] = 0` — and Convert_Buff IS in the save set
# (GS_SAVE(Convert_Buff) / GS_LOAD(Convert_Buff) in src/netplay/game_state.c).
# A hardware-derived, unsaved value writing a saved array is the shape this
# allowlist exists to catch.
#
# It does not bite today, for three independent reasons, all verified:
#   1. the in-battle route (Button_Config_in_Game) needs task[TASK_MENU].r_no[0]
#      == 1, set only inside Setup_Pause/Setup_Come_Out, and the whole
#      Pause_Task is gated `Mode_Type != MODE_NETWORK` (system/pause.c);
#   2. the options-menu route needs TASK_MENU alive, and setup_vs_mode calls
#      cpExitTask(TASK_MENU) (src/netplay/netplay.c) before the session runs;
#   3. even if reached, the write is the literal constant 0, and setup_vs_mode
#      already does SDL_zeroa(Convert_Buff), so it would be a no-op for the
#      session's duration. (Button_Config_Tr is training-only.)
#
# VOIDING CONDITION — delete this entry and re-open the finding if ANY of:
# Pause_Task's `Mode_Type != MODE_NETWORK` gate is relaxed; TASK_MENU is kept
# alive across setup_vs_mode; setup_vs_mode stops zeroing Convert_Buff; or the
# menu.c write stops being a constant 0. It is admitted on unreachability,
# which is a weaker guarantee than the write-only ones above and can be
# revoked by an edit in a file nobody would think to check against this list.
#
# MEASURED 2026-08-30 (task #138) — DE-ALLOWLISTED AND RUN, and it behaves
# exactly as the unreachability argument predicts. Same command and build as
# the mts_ok block above, with only the ppwork line removed: divergent 4 -> 5,
# feedback 0. ppwork surfaces DIVERGENT (not DIVERGENT+FEEDBACK — it is not a
# GS_SAVE symbol) in ryu-ken-basic-exchange only, first frame 639, last 1477,
# 270 divergent frames. A large, persistent divergence with no consumer moving
# is what "move_pulpul_work drains once per replayed logic frame" looks like
# from the outside. makoto-sa3-super does not exercise it at all (its vib_req
# is quiet too). Convert_Buff is byte-identical on all 1500 frames of both.
#
# ALL THREE VOIDING GATES RE-VERIFIED AT TIP on 2026-08-30, since this entry is
# only correct for as long as they hold:
#   1. Pause_Task still opens `!nowSoftReset() && Mode_Type != MODE_NETWORK &&
#      !Is_Training_Mode(Mode_Type)` (system/pause.c) — narrower than when the
#      condition was written, not wider.
#   2. setup_vs_mode still calls cpExitTask(TASK_MENU) (src/netplay/netplay.c).
#   3. setup_vs_mode still does SDL_zeroa(Convert_Buff), and Button_Config_Sub's
#      two writes (menu/menu.c) are still the literal constant 0.
# The dispatch chain in the note also still holds: Menu_Jmp_Tbl[1] is In_Game
# and In_Game_Jmp_Tbl[2] is Button_Config_in_Game (menu/menu.c), so reaching the
# write needs task[TASK_MENU].r_no[0] == 1 with TASK_MENU alive.
ppwork                     # io/pulpul.c rumble device work — REASON FALSIFIED, SEE THE CORRECTION ABOVE. ok_dev gates a write into GS_SAVE(Convert_Buff); admitted on NETPLAY UNREACHABILITY, not on write-only-ness. Honour the voiding condition — all three gates re-verified at tip 2026-08-30 (task #138), and the de-allowlisted run confirms DIVERGENT-without-FEEDBACK
vib_req                    # io/pulpul.c rumble request queue. Reason corrected 2026-08-30: nothing in the pad pipeline reads it. Seven occurrences total, of which exactly two are reads, both `pulpul_request(N, vib_req[N][0])` inside pulpul_request_again; pulpul_request returns void and writes only vib_req and ppwork[id].p[prio]. No engine, effect, menu or netplay file reads it. Both callers of pulpul_request_again (system/pause.c, menu/menu.c) are unreachable during a session anyway. VERIFIED write-only 2026-08-30

# --- per-tick scratch re-derived before every read --------------------------
oya_adrs                   # effect/effb3.c:19 parent pointer, assigned at the top of effect_B3_move every tick (effb3.c:32) before any read
oya_p                      # effect/effb9.c:17 parent pointer, assigned at the top of effect_B9's move every tick (effb9.c:22) before any read

# --- training-UI data (inactive as gameplay input in VS/netplay) -----------
tr_data                    # ui/sc_data.h TrainingData2 display tables; training-menu/UI consumer only
