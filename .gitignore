# Python

.venv
venv
__pycache__

# macOS

.DS_Store

# IDEs

.vscode
.cache

# Project

/build
/build-*
temp
third_party
# Task #81: per-invocation source staging for worktree ARM builds. Created
# under the checkout the build container is bind-mounted from, and removed on
# exit; tracked here so a killed build cannot dirty `git status`.
/.build-src
states
rom
dump
artifacts/mister-port/logs/
artifacts/mister-port/perf/*.json
!artifacts/mister-port/perf/baseline.json
artifacts/mister-port/perf/bolt-*.json

# Quartus FPGA build artifacts (vendor/Menu_MiSTer)

vendor/Menu_MiSTer/db/
vendor/Menu_MiSTer/incremental_db/
vendor/Menu_MiSTer/output_files/
vendor/Menu_MiSTer/build_id.v
vendor/Menu_MiSTer/c5_pin_model_dump.txt
vendor/Menu_MiSTer/jtag.cdf
